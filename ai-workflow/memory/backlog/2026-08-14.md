# 2026-08-14 — TASK-004 재방문 (D-127~D-133)

## 1. 환경

- **worktree**: main (clean, 7 worktree `analysis/<name>-v2` 모두 merge 완료 + 정리)
- **시작**: 사용자 "세월이 많이 지나서 레퍼런스들이 발전을 많이 했어. 다시 조사하자"
- **선택된 옵션 (yklee)**: (1) 전체 7-doc 재조사 + 결론 갱신

## 2. 작업 D-127~D-133

### 2.1 06-09 이후 발전량 (실측, prompt 검증 후)

| reference | 새 commit 수 (06-09~08-14, 66일) | default branch | 최신 release/tag | 비고 |
|---|---|---|---|---|
| opencode | 1,457 | `dev` | v1.18.18 | TUI 안정화 + 모델 cache fix |
| aider | 0 | `main` | v0.86.3.dev | 안정 (정직) |
| codex | 1,996 | `main` | - | app-server + Guardian V2 + Skill validation + Luna sampler + interrupted turn recovery |
| headroom | 106 (실측, prompt 의 1085 = hallucination) | `main` | v0.23.0 (06-04) + Unreleased | RTK=v0.22.4 (D-130 prompt correction) |
| goose | 661 | `main` | - | ACP provider 다수 + 보안 강화 + Recipe/Slash + 8 provider 추가 |
| gemini-cli | 130 | `main` | v0.55.1 | TOML extensions + MCP OAuth refresh + Capacity Exhaustion terminal + caretaker agent |
| claude-code | 594 (실측, prompt 의 66 = 자동화 commit 만) | `main` | - | 자동화 commit 다수, D-34/D-40 §11.2 정합 |

### 2.2 워커 운영 (D-16/D-73 lesson 적용)

- **D-16 chunked write**: 1 plan = 1 reference. 7 worktree (`analysis/<name>-v2` branch) 격리.
- **D-73 prompt lesson**: Worker prompt 머리에 cross-session grep 1-liner.
- **D-73 §3 hallucination 회귀**: headroom D-130 prompt 의 "RTK=v0.23.0" 가 hallucination — 실측 = v0.22.4 변경 (PR #493/494, 2026-05-25). 워커가 §16.14 에 정직 명시. **lesson**: commit ref / module name 은 upstream source 에서 직접 확인 필수.
- **워커 출력**: 모두 1 commit + 7 reference 영향 분석 (+1,987 lines total). 메모리 sync 별도 task.
- **순차 실행 (rate limit 회피)**: 첫 병렬 spawn = 4 fail (rate limit 2062 + serialization error). 순차 재시도 = 7/7 완료. aider / claude-code / codex / goose / headroom / gemini-cli / opencode 순서로 진행.

### 2.3 D-127 (opencode v2)

- **+297 lines append**: §15 v2 Changelog (15 commit 선정, 핵심 PR 11개 + 부수 4개) + §16 영향 분석 (10개 항목, v1 필수 3개 + v1.5+ 4개 + v2+ 3개) + §17 v2 메타
- **commit**: `6057dbb` on `analysis/opencode-v2`
- **핵심 영향**: (a) reasoning effort 표준화 → §5.5 LLM wire format (b) **session retry jitter cap** → §5.5 router (v1 즉시) (c) Copilot PDF detect → §5.5 multimodal (v2+) (d) R2 data catalog → §5.13 observability (v2+) (e) Kimi prompt by provider → §5.5 provider (v1.5+)

### 2.4 D-128 (aider v2)

- **+128 lines append**: §15 (0 commit 정직 명시 + v1 패턴 1:1 정합) + §16 (영향 0, 결정 변경 불요)
- **commit**: `e02853c` on `analysis/aider-v2`
- **결론**: aider 안정. v3 revisit = 5-triggers + 90일 cadence (2026-11-12 추정)

### 2.5 D-129 (codex v2)

- **+523 lines append**: §15 (15 PR 핵심 excerpt) + §16 (6 영향 영역 매핑)
- **commit**: `eec2c88` on `analysis/codex-v2`
- **핵심 영향**: (a) **effective permission** → §5.4 permission mode 의 v0 회귀 차단 (**P0 즉시**) (b) Skill validation → §5.14 skill system 1차 cycle (c) interrupted turn recovery → §5.10 LoopRunner (d) Luna sampler → §5.5 reasoning model (e) sandboxed streaming → §5.10 Bash tool sanitize

### 2.6 D-130 (headroom v2) — 핵심 결과

- **+207 lines append**: §15 v2 Changelog (v0.23.0 8 핵심 + Unreleased §2-§4 + CVE + tag format) + §16 영향 분석 (9 follow-up) + §16.12 D-66/D-67/D-68 tract 재평가
- **commit**: `f6cbc30` on `analysis/headroom-v2`
- **D-130 prompt correction**: 1085 commit → 실측 0 commit. RTK ↔ v0.23.0 link → hallucination (v0.22.4 의 변경). 주석 §16.14 에 lesson 명시.
- **§16.12 핵심**: **D-66/D-67/D-68 tract 재평가 REJECT** (Rust 측 안정성 ↑ = RTK(v0.22.4) + Rust proxy metrics(headroom 자체) → ONNX 무관). v2.0 ONNX 백로그 = **OOS 유지**.
- **즉시 follow-up 2 commit**: (a) SqliteMemoryStore FTS5 schema + `workspace_path` 컬럼 (~150 lines, D-99 의 6 integration test + 1 신규 test). (b) Memory fail-closed — `MemoryStore::open()` 의 `~/.myharness/memory/` 부재 시 명확한 에러 + 안내 (~80 lines, D-98 의 7 ndjson_* test 회귀 0).

### 2.7 D-131 (goose v2)

- **+173 lines append**: §15 v2 Changelog (ACP 확장 + TUI + 보안 + Provider 8종 + Loop 안정화 + Recipe/Slash + 의존성) + §16 영향 분석 (6 영향 축)
- **commit**: `1a86b44` on `analysis/goose-v2`
- **핵심 영향**: (a) ACP SDK 0.12 추가 → §5.5 LLM client (v2+, feature `acp`) (b) **provider 50→58 declarative + OAuth device flow 2-tier** → §5.5 (v1.5+) (c) Recipe/Slash → §5.14 Skill/MCP (d) Loop 안정화 → §5.10 LoopRunner

### 2.8 D-132 (gemini-cli v2)

- **+391 lines append**: §15 (v0.45→v0.55.1 10 minor 핵심 10 PR) + §16 (7 영향)
- **commit**: `f500f87` on `analysis/gemini-cli-v2`
- **핵심 영향**: (a) tool call formatter → §5.5 wire format (b) Cloud Workstations OAuth → §5.5 D-51 OAuth (c) **MCP OAuth token refresh with stored client ID** → §5.5 W15.b 자동 refresh (d) TOML extensions 표준 → §5.14 (e) ingestion workflow → §5.13 LLM Wiki 자동 ingest

### 2.9 D-133 (claude-code v2) — D-34/D-40 검증

- **+266 lines append**: §15 (594 commit 작업자 분류 — 자동화 88% + docs + security + oncall + CI + plugin) + §16 (7 commit + 영향 매트릭스 §5.x = 영향 0) + §17 D-34/D-40 §11.2 정합 검증
- **commit**: `c7b03f1` on `analysis/claude-code-v2`
- **D-34/D-40 §11.2 잠금 정합 검증**: 06-09 이후 claude-code 변경 = 우리 §5.6/§5.14/§5.4/§5.5 영향 0. D-40 의 §11.2 완전 제거 결정이 정합이었음 사후 확인. **결정 변경 불요**.

## 3. main merge

- 7 worktree → 7 merge commit (--no-ff). main = `8782abf` (opencode merge 마지막).
- 7 reference commit + 7 merge commit = **14 commit main**.
- 7 worktree `git worktree remove --force` + 7 branch `git branch -D` 정리.

## 4. 누적 결정

- 69 → **76** (D-127~D-133, 7 신규). state.json decisions.decided.length = 76.

## 5. 다음 1순위 (yklee 결정 시)

- **(a) D-130 즉시 follow-up 2 commit** — CCR + Memory fail-closed (즉시)
- **(b) D-130 §16.3 cli wrap** → TASK-005-2 v1.5+ Sub-task 2 (provider-auto-config Skill) 와 동시 차용
- **(c) D-127 §16.c session retry jitter cap** — §5.5 router 에 즉시 적용 (opencode 의 5-tuple 기반, ~50 lines)
- **(d) D-129 §16.2 effective permission** — §5.4 permission mode 의 v0 회귀 차단 (~100 lines)
- **(e) D-131 §16.4 provider 50→58** — §5.5 provider 등록 (v1.5+)
- **(f) D-132 §16.7 TOML extensions** — §5.14 skill system 표준 (v1.5+)
- TASK-002 도메인 명령 / OAuth real flow / TUI shell / cargo hygiene / Lark parser / block-aware insert/replace (이전 9 옵션)

---

## 6. D-134 — grok-build 8번째 reference (같은 날 2차 세션)

- **trigger**: "reference에 grok build도 추가하자" + 커스텀 하네스 검토 → 사용자 옵션 3 (분석만 깊게)
- **실측**: `/Users/yklee/repos/grok-build` (Apache 2.0, Rust 1.92, workspace 79, `*.rs` 1,362,619). 설치 `grok 1.0.3`. 외부 PR 거부
- **산출물**: `docs/references/grok-build.md` 14섹션 + §15. README 7→8 doc. CONCEPT §12 링크
- **닫힌 사실 (미결정)**: 5 components 1:1. overlay = `grok agent stdio` + `--plugin-dir`. 소스 포크 비권장. 독립 런타임은 goose
- **보류**: overlay / grok-fork / goose-fork. 다음 세션 진입점
- 누적 결정 76 → **77**

---

## 7. 2차 세션 (TASK-005-2 v2.0 진입 옵션 제시, 결정 없이 종료)

- **trigger**: "이전 작업 확인하자" → workflow 메모리 restore → "A부터 진행하자" → TASK-005-2 v2.0 옵션 5개 제시 → "작업 종료하자"
- **실제 작업**: 0건 (코드/문서 변경 없음, 결정 ID 불요)
- **활동**:
  1. workflow 메모리 restore (`session_handoff.md` + `state.json` + `work_backlog.md` + `backlog/2026-08-14.md` 의 D-134 메모 확인)
  2. CONCEPT.md §5.7 (Plugin 4-계층 구조) + §6 (TASK-005-N 로드맵) 확인
  3. Plugin 4-계층 Sub-task 2 분해 옵션 5개 제시: **(a) Plugin 인프라 (loader + manifest + dispatcher)** / **(b) commands/ (slash commands)** / **(c) skills/ (provider-auto-config 정식)** / **(d) agents/ (Sub-agent SDK)** / **(e) hooks/ (Hook markdown rule)**
  4. 진입 옵션 묶음 4개 제시: **A1** (a 단독) / **A2** (a+b) / **A3** (a+c) / **A4** (sub-task 분할 재논의)
- **yklee 결정**: 없음 (종료). 옵션 A1~A4 미선택
- **다음 세션 진입점**: TASK-005-2 v2.0 Plugin 4-계층 Sub-task 2 (옵션 A1~A4 중 yklee 선택) 또는 D-134 grok-build overlay/포크 결정
- **상태 보존**: main = `4eec8e8` (D-134 grok-build 8번째 reference). git status clean. 결정 카운트 77 그대로

---

## 8. D-135 — overlay 확정 + 문서 재구성 (같은 날 3차)

- **trigger**: 진입점 A 꼬임 진단 → overlay 브리핑 → "A 안에 따라 재구성, 문서부터"
- **결정**: 제품 경로 = grok overlay. 자체 Plugin 인프라 A1~A4 = **OOS**
- **산출**: `docs/architecture/DETAILED_DESIGN_OVERLAY.md` + CONCEPT §0 외 정합 (README / PROFILE / MiniMax / AGENTS / REFERENCES / grok-build §15 / INITIAL_DESIGN·REQUIREMENTS 배너 / development_log)
- **코드**: 0. 다음 = PR-1 plugin 스캐폴드
- 누적 결정 77 → **78**
