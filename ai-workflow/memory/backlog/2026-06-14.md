# Backlog — 2026-06-14

## Sub-task 1 완료 (D-98 + D-99) — TASK-005-2 v2.0 Plugin 4-계층 Layer 1 Auto Memory

- 시점: 2026-06-14
- project: my-harness
- 의도: TASK-005-2 v2.0 Plugin 4-계층 의 첫 sub-task (Layer 1: Auto Memory 강화) 완료. conservative extension 패턴 (NDJSON 보존 + SqliteMemoryStore opt-in).
- 산출물: 2 commit / 13 file / +1233 -313. main = `c64d0ff`.

### D-98 (57db117) — Commit A: Auto Memory pure refactor

- 기존 `myharness/crates/context/src/auto_memory.rs` (293 lines 단일 NDJSON append-only, D-46) → 4 file 모듈 분할
  - `auto_memory/mod.rs` (~261 lines) — facade + AutoMemoryConfig + sync/async wrapper + tokio Handle bridge
  - `auto_memory/types.rs` (~102 lines) — MemoryKind/Record/Query/Hit/Error
  - `auto_memory/store.rs` (~269 lines) — MemoryStore trait + NdjsonMemoryStore adapter + 7 ndjson_* tests
  - `auto_memory/query.rs` (~59 lines) — MemoryQuery builder helpers + bm25_normalize
- lib.rs re-export 확장 (AutoMemoryConfig, MemoryBackend, MemoryQuery, MemoryHit, MemoryError, MemoryStore)
- back-compat 보존: AutoMemory::new/with_base/append/recent/to_system_prompt_section 시그니처 + 동작 동일
- config.rs::ContextOrchestrator caller 변경 없음
- Cargo.toml 변경 없음 (Commit B 의 영역)
- **adaptation**: `futures::executor::block_on` → tokio Handle bridge (Cargo.toml 변경 금지 제약)
- **adaptation**: `MemoryError::Sqlite(String)` placeholder (rusqlite 미설치, Commit B 에서 `#[from]` 으로 tighten)
- commit scope: 5 file / +695 -294

### D-99 (c64d0ff) — Commit B: SqliteMemoryStore (rusqlite FTS5 + BM25)

- `auto_memory/sqlite_store.rs` (303 lines) — SqliteMemoryStore + MemoryStore trait impl (6 method) + tokio::task::spawn_blocking
- `auto_memory/query.rs` 확장 (+16 lines) — FTS5 MATCH expression builder
- `auto_memory/mod.rs` 확장 (+53 -19) — Sqlite variant 활성화 + block_on bridge 단순화
- `auto_memory/types.rs` 확장 (+6 -2) — MemoryError::Sqlite `#[from] rusqlite::Error` tighten
- `tests/auto_memory.rs` (119 lines, 6 integration tests):
  - back_compat_ndjson_default
  - back_compat_legacy_records_load
  - sqlite_append_and_query_match
  - sqlite_cross_session_recall
  - sqlite_bm25_ranking
  - sqlite_kind_filter
- Cargo.toml: workspace `rusqlite = { version = '0.31', features = ['bundled'] }` + context crate `rusqlite = { workspace = true }`
- FTS5 virtual table schema (memory + memory_fts + 3 triggers, porter unicode61 tokenizer)
- bm25(memory_fts) ranking
- default backend = NDJSON (back-compat), opt-in via `MYHARNESS_MEMORY_BACKEND=sqlite`
- clippy 1 `#[allow(clippy::collapsible_if)]` on query method (let-chain 회피)
- commit scope: 8 file / +538 -19

## 검증

- main = `c64d0ff` (Sub-task 1 Commit B)
- cargo build --workspace: clean (1.77s)
- cargo clippy --workspace --all-targets -- -D warnings: clean (0 warning)
- cargo test --workspace: **453 pass + 0 fail + 2 ignored** (baseline 447 + 6 Commit B integration, Commit A 의 7 ndjson test 는 context crate 의 55 안에 포함, 0 regression)
- MYHARNESS_MEMORY_BACKEND=sqlite smoke test: 6/6 pass
- NDJSON default path unchanged: 7 ndjson_* tests pass + 2 ndjson back_compat integration pass

## 누적 결정 갱신

- my_harness: 28 → **30** (D-98 + D-99)
- state.json decisions.decided.length: 45 → **47**
- 진짜 누적 SSOT: 47 entry (my_harness 30 + wiki 13 = 43, but 47 = state.json SSOT)

## 다음 결정 (yklee)

1. **Sub-task 2 시작** (provider-auto-config Skill 정식, llm + cli 영향, MEDIUM effort)
2. **Sub-task 3 시작** (Plugin Installer, core + cli 대폭 확장, HIGH effort)
3. **Sub-task 4 시작** (Marketplace, MEDIUM effort, Sub-task 3 prerequisite)
4. **세션 종료** (Sub-task 1 완료 상태로 main 머지)

## task_list 정리

- opencode task_list 의 T-5733f439 (Plugin 4-계층 현재 상태 조사), T-72666e53 (plan), T-0d2bb871 (user confirm), T-2dac6379 (선택 sub-task implementation), T-0e62016c (검증), T-e03b621d (Commit B) 모두 completed 처리. 다음 세션 시작 시 정리 또는 자동 만료 대기.