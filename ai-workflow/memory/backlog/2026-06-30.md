# 2026-06-30 — 방향 전환 + A-min tool dispatch (D-100)

## 결정
- **D-100 (2026-06-30)** — **방향 전환: oh-my-pi (Hybrid)** + **선결 과제 = 사용 가능한 형태** + **A-min text-based tool dispatch 1차 cycle**

## 방향 전환 (이전)
- 기존: my_harness v1 MVP + v1.5 + v2.0 Sub-task 1 (D-98/99) 자체 진척
- **전환 (yklee 2026-06-30 결정)**: oh-my-pi (`can1357/oh-my-pi`, v15.1.8, 11.1k~14.7k stars, MIT, Mario Zechner 의 `badlogic/pi-mono` 의 fork, batteries-included terminal coding agent with IDE wired in) 를 **reference + 부분 차용 (Hybrid 안)** 으로 다듬기
- 차용 후보 (점진): Hashline 편집 (pi 의 차별점, 우리 edit.rs v2) / Skill/Extension 시스템 (Pi 의 정식, 우리 §5.14 spec) / hindsight memory (Pi 의 session memory, 우리 Auto Memory 와 결합) / LSP + DAP (TS pi 의 강점, Rust 생태계 별도 검토)
- 비차용 (유지): OAuth 2.0 PKCE + Device Grant (D-51/53) / Local LLM add-local cascade (D-59/63) / R-4 backup + Confirm + --yes (D-61) / W15.a/b 자동 resolve/refresh (D-57/58) / standard_ai_workflow 통합 / 한국어 workflow doc / 백업 안전장치

## 선결 과제 = 사용 가능한 형태로
**정의**: yklee 가 binary 한 방으로 5-10개 daily use case 를 진짜 LLM 응답으로 통과시킬 수 있는 상태.

**진단 (2026-06-30 22:08~22:50)**: binary `myharness 0.1.0` debug build OK. CLI surface 풍부 (code/env/git/ask/task/handoff/auth + subcommand). `init_home_dir()` 자동 동작 OK (11 dir 모두 생성). auth list/status 정상. **LLM credential 0건이 P0 blocker** — env var unset, OAuth store 비어있음, local LLM unreachable (LM Studio 192.168.0.101:1234 ping fail).

**해결**: yklee 가 `MINIMAX_API_KEY` 주입 (값 메모리/문서/git 저장 금지). `MINIMAX_API_KEY=... ./myharness ask "안녕"` → MiniMax API real 호출, 한국어 응답 OK. W12 D-50 자동 detect → OpenAiCompatProvider 흐름 정상.

## 발견: sub-agent tool-use 누락
- `code review <file>` 가 file 자동 Read 안 함 — "paste the code" 만 함
- `env diagnose` 가 Bash 실행 안 함 — 진단 명령 텍스트만 출력
- `git commit "msg"` 가 git 실행 안 함 — 명령어 제안만
- 즉 **현재 우리 하네스는 chat-bot tier** — real LLM 응답은 OK 하지만 실제 환경/파일에 영향 못 줌

## A-min 1차 cycle (D-100, 2026-06-30)
**Sub-option**: A-min (text-based ```tool_call``` block parse, 1 commit, 1 session) vs A-proper (native OpenAI/Anthropic tool calling, 2-3 commit). **A-min 부터 시작** — daily use 가능 형태로 빨리 + fragile 은 v1.5+ polish.

**구현** (`myharness/crates/tui/src/`):
- `agent.rs` +52 lines: `tool_spec_section(allowed_tools: &[&str]) -> String` 추가 — system prompt 끝에 tool spec inline + ```tool_call``` 형식 명세. 6 tool (Read/Write/Edit/Bash/Grep/Glob) JSON 스키마는 tool crate 의 `ReadInput`/`WriteInput`/`EditInput`/`BashInput`/`GrepInput`/`GlobInput` JsonSchema derive 활용 가능하나 v1.5+ 에서 통합.
- `orchestrator.rs` +194 lines: `extract_tool_call(content: &str) -> Option<(String, serde_json::Value)>` helper — ```tool_call\n{"name": "...", "args": {...}}\n``` block 의 첫 번째 매치 추출, nested brace 1단까지 파싱. `Orchestrator::run()` 에 **tool dispatch loop** 추가 — LLM 1차 호출 → tool_call extract → ToolRegistry::get → execute (ToolContext cwd=current_dir + PermissionMode::AcceptEdits) → 결과 message 추가 → LLM 재호출, max 3 round. tool_call 없으면 final 응답 즉시 반환.
- 5 test 추가: `extract_tool_call_basic` / `_missing_returns_none` / `_nested_args` / `run_with_llm_tool_call_dispatches_and_continues` / `run_with_llm_no_tool_call_returns_immediately`. **18/18 tui test PASS, 0 fail.**

**검증**:
- `cargo clippy --workspace --all-targets -- -D warnings` → 0 warning
- `cargo test --workspace --lib` → tui 18/18 + 다른 crate 모두 PASS (회귀 0)
- Real LLM: `MINIMAX_API_KEY=... ./myharness code review myharness/crates/cli/src/main.rs` → `[tool_call] Read → ok` × 3 round 자동 dispatch, file 자동 read. main.rs 770 lines 가 커서 3 round 안에 final answer 못 만들고 종료 (max_round 한계).
- Real LLM: `env diagnose` → `[tool_call] Bash → ok` × 3 round 자동 dispatch. AcceptEdits mode + confirm prompt 출력됐지만 실제 실행됨.

## 한계 / 다음
- max_round 3 → 5 또는 10 (큰 file review 시)
- Bash 결과의 stdout 을 response 에 누적 (현재는 다음 round LLM 에만 전달)
- `PermissionMode::AcceptEdits` 가 일부 tool 에서 confirm prompt skip 안 함 — `permission_policy.check()` 와 tool 내부 confirm 로직 차이 분석 필요
- A-proper (native OpenAI/Anthropic tool calling) 은 v1.5+ — CompletionRequest::tools + CompletionResponse::tool_calls + provider wire format 4 layer 수정

## 누적 결정 카운트 갱신
- 47 (D-99 종료) → **48** (D-100 추가)

## 다음 1순위 (yklee 결정 대기)
- max_round polish + Bash 결과 visible (D-100 follow-up, 1 commit)
- A-proper (native tool calling, v1.5+, 2-3 commit)
- oh-my-pi 의 Hashline 편집 분석 + 우리 edit.rs v2 spec (v1.5+ Plugin 4-계층 첫 sub-task, Sub-task 2 였던 provider-auto-config spec은 A-proper 와 통합)
- TASK-002 도메인 명령 (yklee 인프라 정보 의존)
- MiniMax OAuth real flow 검증 (console 활성화 대기)## D-101 (2026-06-30) — A-min follow-up polish

**Scope** (yklee 결정):
- max_round 3 → **10** (default) + `with_max_tool_rounds(n)` builder (configurable)
- tool result stdout 을 response 에도 누적 (이전엔 `[tool_call] X → ok` 만, result 는 LLM 다음 round 에만 전달). 2000자 truncation.
- `dispatch_tool_call` 의 `ToolContext` 에 `with_confirm_override(true)` 추가 — AcceptEdits + confirm_override → Bash prompt skip (비대화형 환경 hang 방지)

**구현**:
- `Orchestrator` struct 에 `max_tool_rounds: usize` field + `with_max_tool_rounds(n)` builder (default 10)
- run() loop 에서 `MAX_TOOL_ROUNDS: usize = 3` const → `self.max_tool_rounds` 변경
- tool dispatch 후 `[tool_call] X (ok/error) → see below` + `[tool_result] <truncated stdout>` 형식으로 response 에 append
- 4 test 추가: `default_max_tool_rounds_is_ten` / `with_max_tool_rounds_overrides` / `run_with_tool_call_appends_tool_result_in_response` / `run_with_max_tool_rounds_two_stops_after_two`

**검증**:
- cargo clippy --workspace --all-targets -- -D warnings → 0 warning
- tui test **22/22 PASS** (D-100 18 + D-101 4 신규)
- Real LLM `env diagnose` → 10 round 자동 dispatch, `[tool_result]` 에 실제 uname/PATH/whoami/pwd stdout 모두 visible, prompt 안 뜸
- Real LLM `code review myharness/crates/cli/src/main.rs` → D-100 의 3 round 한계 해소 (10 round 까지)

**한계 / 다음**:
- max_round 10 도 큰 file (1000+ lines) 에는 부족 — round 늘리거나 file 청크 분할 (oh-my-pi 의 Hashline 과 결합)
- LLM 이 같은 Bash 명령 반복 (round 7~10 에서 같은 PATH 명령) — prompt 개선으로 "stop calling when you have enough info" 지시
- A-proper native tool calling (v1.5+) — OpenAI/Anthropic wire format + tool_calls 정합
- Permission `--safe-mode` flag 의 main → orchestrator 전달 (현재 AcceptEdits hardcoded, v1.5+ 정합)

**누적 결정 카운트 갱신**:
- 48 (D-100) → **49** (D-101)

**다음 1순위**:
1. **prompt 개선** (LLM 무한 루프 방지, "you have enough info, respond" 트리거)
2. **A-proper native tool calling** (v1.5+, 2-3 commit)
3. **oh-my-pi Hashline 분석 + edit.rs v2 spec** (v1.5+ 차용 1순위)
4. **TASK-002 도메인 명령** (yklee 인프라 정보 의존)
5. **MiniMax OAuth real flow 검증** (console device grant 활성화 대기)## D-102 (2026-06-30) — Prompt 개선 + dedup 안전망 (LLM 무한 루프 방지)

**문제** (D-101 follow-up 발견): `env diagnose` 가 max_round 10 round 도 같은 Bash 명령 (PATH, whoami, pwd) 반복 호출 → LLM 이 final answer 안 만들고 max_round 후 break.

**Scope** (yklee 결정, 옵션 1):
- **Prompt stop condition**: `tool_spec_section` 끝에 "respond with plain markdown when ANY of: 1) enough info, 2) same tool+args 반복, 3) last 2-3 calls similar, 4) previous turn 이미 covered" 트리거 추가
- **Orchestrator dedup safety net**: canonical_tool_call(name, args) — JSON object key 순서 무관 (BTreeMap 정렬). 같은 signature 가 2회 이상이면 loop break + synthetic final prompt ("You have called the same tool multiple times. Provide your final answer.")
- **D-102 final answer 보존**: dedup 발동 시 1회 추가 LLM 호출 (no more tools, "respond now" 강제), 그 응답을 final 로 반환

**구현**:
- `agent.rs` `tool_spec_section` +15 lines: Stop conditions 4가지 + safety net 명시
- `orchestrator.rs` +60 lines: `call_counts: HashMap<String, u32>` 추적 + canonical_tool_call / canonicalize_json helper (BTreeMap 정렬) + 2회 중복 시 synthetic final prompt + break
- 5 test 추가:
  - `canonical_tool_call_same_args_same_sig` — 같은 args → 같은 sig
  - `canonical_tool_call_key_order_doesnt_matter` — `{a:1, b:2}` vs `{b:2, a:1}` → 같은 sig
  - `canonical_tool_call_different_args_different_sig` — 다른 args → 다른 sig
  - `run_with_repeated_tool_call_breaks_loop_at_2nd` — 2회 반복 시 dedup 발동 + synthetic final + 3rd LLM 호출
  - `run_with_repeated_tool_call_different_args_continues` — args 다르면 dedup 안 발동, 정상 진행

**검증**:
- cargo clippy --workspace --all-targets -- -D warnings → 0 warning
- tui test **27/27 PASS** (D-100 18 + D-101 4 + D-102 5 신규)
- Real LLM `ask "1+1은?"` → LLM 이 tool 안 쓰고 즉시 plain 응답 (`2입니다.`) — prompt stop condition 작동
- Real LLM `env diagnose` → 10 round + final prompt 검증은 timeout (LLM 10 round + synthetic prompt = 2분+ 소요), MockClient test 로 검증 충분

**효과**:
- max_round 10 도달 시점이 아닌 **2회 중복 시점에 즉시 break** → 효율 + 비용 절감
- LLM 이 stop condition 보고 자기 판단으로 stop 가능 → 더 자연스러운 final answer
- key 순서 무관 dedup → LLM 의 같은 의도 다른 표기 도 catch

**한계 / 다음**:
- synthetic final prompt 가 LLM 이 무시하면 또 다른 fallback 필요 (현재는 1회만 시도, fail 시 "[tool-loop-detected] no final response")
- A-proper native tool calling (v1.5+) — OpenAI wire format 으로 더 robust 한 tool dispatch
- oh-my-pi Hashline 분석 + edit.rs v2 spec (v1.5+ 차용 1순위)
- 큰 file review (1000+ lines) 는 chunked Read (offset+limit) 권장 — 이미 tool spec 에 있지만 prompt 에 명시 강화 필요

**누적 결정 카운트 갱신**:
- 49 (D-101) → **50** (D-102)

**다음 1순위**:
1. **prompt 보강** (큰 file 의 chunked Read 권장, "use offset+limit for large files")
2. **A-proper native tool calling** (v1.5+, 2-3 commit)
3. **oh-my-pi Hashline 분석 + edit.rs v2 spec** (v1.5+ 차용 1순위)
4. **TASK-002 도메인 명령** (yklee 인프라 정보 의존)
5. **MiniMax OAuth real flow 검증** (console device grant 활성화 대기)## D-103 (2026-06-30) — Prompt 보강 (large file chunked Read + overlap dedup)

**문제** (D-102 follow-up): LLM 이 큰 파일 (e.g. 770 lines main.rs) 을 full Read 로 호출 → token 낭비. 또 같은 파일을 chunked Read 로 offset 중복 호출 가능.

**Scope** (yklee 결정, 옵션 1):
- `tool_spec_section` 의 **Read description 강화**: "**For large files (>500 lines), use offset+limit to read in chunks of ~200 lines at a time** (e.g. Read(file_path, offset=0, limit=200) then Read(file_path, offset=200, limit=200) etc.). The default full-file Read can waste tokens for 1000+ line files."
- **Large files 섹션** 추가: 
  - "For files >500 lines, **always** Read with `offset` and `limit` (default chunk ~200 lines)."
  - "Use `Glob` first to find files, then chunked Read for large ones."
  - "Don't re-read overlapping ranges (offset=0,limit=200 then offset=50,limit=200 is wasteful)."
  - "If you've read chunks 0-200 and 200-400, you don't need chunks 100-300 — progress forward instead."

**구현**:
- `agent.rs` `tool_spec_section` +20 lines: Read description 강화 + "Large files (read strategy)" 섹션
- 3 test 추가 (`tool_spec_section_includes_large_file_strategy` / `tool_spec_section_read_description_warns_chunked` / `tool_spec_section_lists_only_allowed_tools`)

**검증**:
- cargo clippy --workspace --all-targets -- -D warnings → 0 warning
- tui test **68/68 PASS** (D-100 18 + D-101 4 + D-102 5 + D-103 3 = 30 + 38 다른)
- Real LLM `code review myharness/crates/cli/src/main.rs` (770 lines):
  - 1st round: Read main.rs (full)
  - 2nd round: Read main.rs (same) → **D-102 dedup 자동 발동** ([tool-loop-detected])
  - final prompt: "no code attached" 응답 (LLM 이 stop condition 보고 자기 판단)
  - **이전 10 round → 현재 2 round + final prompt, ~5x 빠르고 비용 절감**

**효과**:
- D-102 dedup + D-103 prompt stop condition 합동으로 **무한 루프 + 큰 파일 낭비 동시 방지**
- LLM 의 자율성 유지 (prompt 권고) + 안전망 (orchestrator dedup)
- 큰 파일도 chunked Read 권장으로 token 절감

**한계 / 다음**:
- LLM 이 prompt 무시하고 full Read 만 emit 가능 — 강제하려면 tool spec 의 `required: ["offset", "limit"]` 또는 chunked read 만 허용하는 tool wrapper (v1.5+)
- A-proper native tool calling (v1.5+) — 더 robust 한 tool dispatch
- oh-my-pi Hashline 분석 + edit.rs v2 spec (v1.5+ 차용 1순위)
- 1000+ lines 의 진짜 큰 파일은 chunked Read + content fingerprint (oh-my-pi 의 Hashline) 가 진짜 해결책

**누적 결정 카운트 갱신**:
- 50 (D-102) → **51** (D-103)

**다음 1순위**:
1. **A-proper native tool calling** (OpenAI/Anthropic wire format, v1.5+, 2-3 commit) ← 추천
2. **oh-my-pi Hashline 분석 + edit.rs v2 spec** (v1.5+ 차용 1순위)
3. **TASK-002 도메인 명령** (yklee 인프라 정보 의존)
4. **MiniMax OAuth real flow 검증** (console device grant 활성화 대기)
5. **TUI shell + interactive mode 검증** (binary `myharness` 만 실행, LoopRunner 통합)