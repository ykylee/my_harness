# 2026-07-01 — D-104 (Hashline 1차 cycle) + D-105 (Edit v2 line_anchored mode)

## 0. 환경 (D-105 시작 시)

- **worktree**: main (clean, up to date with upstream at `43bc908` D-104)
- **시작**: 직전 세션이 D-104 완료 후 끊김. OpenCode repair context 로 재개.
- **선택된 옵션 (yklee)**: (a) D-105 Edit v2 line_anchored mode 진행 — 본 세션 작업.
- **D-105 완료 후 main**: `5e39f5e` (dual-push Gitea + GitHub).

---

## 1. 환경 (D-104 시작 시 — 보존)

- **worktree**: main (clean, up to date with upstream at `99a2bad` D-103)
- **시작**: OpenCode runtime repair context (직전 세션 mavis 가 D-103 + "2 가자" 후 끊김) → 이어서 진행
- **선택된 옵션 (yklee)**: (2) oh-my-pi Hashline 분석 + edit.rs v2 spec

## 2. 작업 D-104

### 2.1 why
v1.0 edit.rs = old_string / new_string / replace_all 패러다임. 한계:
- LLM 이 old_string 정확히 복사 필요 (typo 1자 → fail)
- 1000+ lines 파일에서 "어디가 line N 인지" 알 수 없음 (Read raw text 반환)
- stale anchor 감지 불가 (다른 agent / tool / user 가 그 사이 파일 변경했는지 모름)

### 2.2 참조
`/Users/yklee/.bun/install/cache/@oh-my-pi/hashline@15.11.0@@@1/`
- `src/format.ts` — `HL_FILE_HASH_LENGTH = 4`, `formatNumberedLines()` (`{n}:{line}`)
- `src/prefixes.ts` — hashline prefix strip regex
- `src/prompt.md` — 전체 사용자 prompt spec (rules + anti-patterns)
- `src/parser.ts` — Lark grammar parser (D-105+ 에서 미도입 — manual split 만)
- `src/apply.ts` — Patcher (snapshot store + recovery, v2 영역)

### 2.3 차용 결정 (v1.5+ 점진, 9-area)
| area | spec | 차용 시점 | my_harness |
|---|---|---|---|
| LINE:TEXT Read format | ✅ | **D-104** ✅ | 완료 |
| content hash tag (4-hex xxhash32) | ✅ | **D-104** ✅ | 완료 |
| `replace N..M` (concrete lines) | ✅ | **D-105** ✅ | 완료 |
| `replace block N` (tree-sitter) | ✅ | D-106+ v1.5+ | TODO (tree-sitter 0.26 이미 workspace) |
| `delete N..M` / `insert before/after` | ✅ | D-106+ | TODO (D-105 는 empty replacement = delete 만) |
| Multi-section patch parser (Lark grammar) | ✅ | v1.5+ | TODO (D-106+ manual parser 검토) |
| Filesystem abstraction (`InMemoryFs` / `NodeFs`) | ✅ | ❌ v1 은 tokio::fs 유지 | OOS |
| SnapshotStore + 3-way merge recovery | ✅ | ❌ v2 (hindsight memory 결합 시점) | OOS |
| `replace block` 의 tree-sitter resolution | ✅ | ❌ v2 | OOS |

**D-105 완료 = 9-area 중 5 area 점유** (Read v2 2 + Edit v2 3 = LINE:TEXT, content hash, replace N..M).

### 2.4 content hash algorithm 결정

| option | 장점 | 단점 |
|---|---|---|
| sha2 0.11 (workspace 존재) | 새 dep 없음 | xxhash spec 1:1 아님 |
| xxhash-rust 0.8 (신규 dep) | oh-my-pi 1:1 정합 | 새 dep + 빌드 시간 + |
| std::hash::DefaultHasher (Rust built-in) | dep 0 | deterministic ❌ (SipHash randomized per process) |

**결정 = sha2 truncate low 16-bit (4 hex uppercase)**. collision space = 65536, session-scope (단일 파일 ≤ 1MB) 에선 사실상 0. 4 hex `XX{low16:0>4X}` 형식 = oh-my-pi `low16.toString(16).padStart(HL_FILE_HASH_LENGTH, "0").toUpperCase()` 1:1.

### 2.5 normalize 결정

oh-my-pi 와 1:1: line 별 trailing `[ \t\r]+` trim. final newline 별도 보존. CRLF 는 v1 은 LF 만 (BOM/CRLF/Raw NUL 등은 D-105+ safety check 에서 추가).

## 3. 변경

### 3.1 file

| file | 변경 |
|---|---|
| `myharness/crates/tools/Cargo.toml` | `sha2 = { workspace = true }` 추가 (+1 line) |
| `myharness/crates/tools/src/lib.rs` | `pub mod content_hash;` + re-export 4 (compute_content_hash, format_line_anchored, HASH_TAG_LENGTH) |
| `myharness/crates/tools/src/content_hash.rs` | **NEW** (~170 lines, 8 test) — `compute_content_hash` + `normalize_for_hash` + `format_line_anchored` + 8 unit test |
| `myharness/crates/tools/src/read.rs` | v2 — LINE:TEXT format default + metadata 확장 + 4 신규 test |
| `ai-workflow/memory/hashline_v2_spec.md` | **NEW** (~200 lines, 9 section) — spec memo (D-105+ roadmap 포함) |
| `ai-workflow/memory/state.json` | decisions.decided +1 (D-104), in_progress_items +1, current_baseline / current_axis 갱신 |
| `ai-workflow/memory/session_handoff.md` | D-104 bullet 추가 (회귀 / 차용 결정 / 한계) |

### 3.2 test 추가 (12)

**content_hash (8)**:
- `hash_is_deterministic` — 동일 input → 동일 hash
- `hash_changes_with_content` — 1자 변경 → 다른 hash
- `hash_normalizes_trailing_whitespace` — line 끝 trailing space → 영향 없음
- `hash_normalizes_per_line_trailing_whitespace` — 모든 line trailing trim 적용
- `hash_is_uppercase_hex` — `XXXX` 형식 (lowercase ❌)
- `format_line_anchored_basic` — `1:line1\n2:line2\n3:line3`
- `format_line_anchored_with_offset` — start_line=10 → `10:line1\n11:line2`
- `format_line_anchored_no_trailing_newline` — trailing newline 없는 input 처리

**read v2 (4)**:
- `test_read_file` (existing, format 변경 검증) → LINE:TEXT format 검증
- `test_read_emits_content_hash_in_metadata` — metadata 에 `format:line_text` + `hash_length:4` + uppercase 4-char hash
- `test_read_chunked_preserves_absolute_line_numbers` — offset=2,limit=3 → `3:line3\n4:line4\n5:line5`
- `test_read_full_file_matches_chunked_hash` — full vs chunked 동일 content_hash invariant

### 3.3 검증

| check | 결과 |
|---|---|
| `cargo clippy --workspace --all-targets -- -D warnings` | 0 warning (clippy catch 3 → text rewrite: while_let_on_iterator + manual_pattern_char_comparison ×2) |
| `cargo test --manifest-path myharness/Cargo.toml -p myharness-tools --lib` | **62 pass + 0 fail + 0 ignored** (D-103 50 → D-104 62 = +12) |
| `cargo test --manifest-path myharness/Cargo.toml --workspace --lib` | **436 pass + 2 ignored + 0 fail** (D-103 baseline 424 → +12, 회귀 0) |
| `cargo build --manifest-path myharness/Cargo.toml` | clean |
| invariant: full vs chunked 동일 content_hash | ✅ |
| invariant: LINE:TEXT 1-indexed absolute | ✅ |

## 4. 다음 사이클 (D-105+)

### 4.1 ✅ D-105 Edit v2 line_anchored mode (이 spec §5 구현) — **완료**
- JSON schema:
  ```json
  {
    "file_path": "src/main.rs",
    "line_anchored": {
      "start_line": 5,
      "end_line": 7,
      "expected_hash": "A1B2",
      "replacement": "fn main() { ... }"
    }
  }
  ```
- validation 순서: hash check → line range check → replace → re-hash + return ✅
- back-compat: 기존 old_string/new_string mode 보존 (default 가 line_anchored 으로 바뀌지 않음 — explicit opt-in) ✅
- prompt 추가: agent.rs tool_spec_section 의 Edit 줄에 line_anchored mode spec + 사용 예시 + anti-pattern (oh-my-pi tight range 룰) — **D-106+ follow-up**
- **결과**: 10 test 추가 (happy_path / stale_anchor / out_of_range / invalid_range / single_line / preserve_trailing_newline / entire_file / multiline_replacement + apply_line_replacement unit + old_mode_still_works), 1 commit (`5e39f5e`)
- **Spec deviation**: stale anchor 메시지에 `(current hash XXX, expected YYY)` suffix 추가 (LLM drift 규모 즉시 판별, spec prefix 는 그대로)

### 4.2 D-106+ tree-sitter 도입 (v1.5+)
- workspace.tree-sitter = "0.26" 이미 있음
- tree-sitter-rust 만 추가 (또는 multi-language pack)
- `replace block N` operation 구현 (function / impl / struct body 자동 resolution)
- editor prompt 의 "long construct rewrite" use case 해소

### 4.3 D-106+ pure insert/delete (multi-section atomic)
- D-105 의 empty replacement = delete 와 별도
- `insert before/after N` + multi-section atomic 보장 (단일 patch parse)
- grammar.lark 직접 도입 ❌ — 단순 manual parser (Rust nom 또는 split-and-validate)

### 4.4 v2 snapshot store
- hindsight memory 와 결합
- 동일 file 의 동일 hash → in-memory content cache 로 3-way merge recovery
- v2 진입 시점 (Plugin sub-task 4 시점)

## 5. 외부 reference 검증 결정 (cross-project 적용)

D-104 작업 중 발견한 AIDLC 3가지 검증 함정 적용 (mavis memory §1):
1. **regex literal 매칭**: oh-my-pi 의 hash prefix regex (`/^\s*(?:>>>|>>)?\s*(?:[+*-]\s*)?\d+:/`) 는 우리 `format_line_anchored` 에 그대로 안 가져옴 — 우리 v1 은 LLM emit format 만 spec, parser 는 v1.5+ 에서 manual regex 보강
2. **placeholder 미계산**: spec memo §5 Edit v2 의 input schema example = 실제 JSON 직렬화 결과로 검증 (placeholder 1개 = `expected_hash` 4-hex, `start_line/end_line` usize, `replacement` string, `file_path` string)
3. **escape**: spec 의 markdown code fence 안 prompt snippet 그대로 — Edit tool 의 JSON-schema 로 LLM 노출 시 추가 escape 불요 (LLM 직접 인식)

bundle 가능 조건 4개:
- 동일 1차 출처 (`@oh-my-pi/hashline`) — ✅
- 약한 step 의존 (D-104 Read → D-105 Edit 순차 의존, D-106+ 더 약해짐) — ✅ (반대 방향 의존 없음)
- 누적 회귀 0 (D-104 의 12 test 모두 신규, 기존 50 test 회귀 0) — ✅
- my_harness 측 evolution (`tree-sitter 0.26` 이미 workspace) 과의 정합 — ✅ (D-106 시점에 자연 활용)

## 6. 메모리 동기화 (단일 push — Option B 2 commit)

### 6.1 D-104 commit + push
- Commit 1 (code): `feat(tools): D-104 oh-my-pi Hashline 1차 cycle — Read v2 (LINE:TEXT + 4-hex content_hash)` (`43bc908`)
- 1 commit dual-push (Gitea + GitHub)

### 6.2 D-105 commit + push (본 세션)
- Commit 1 (code): `feat(tools): D-105 Edit v2 line_anchored mode — replace N..M with hash check` (`5e39f5e`)
- Commit 2 (memory sync): state.json / session_handoff.md / work_backlog.md / backlog/2026-07-01.md 갱신
- 단일 push = 코드 + 메모리

### 6.3 file scope
- **D-104**: 5 file modify + 1 file NEW (`content_hash.rs` + `hashline_v2_spec.md` + 4 memory) / cargo build clean
- **D-105**: 1 file modify (`edit.rs` 132 → 658 lines, +537 -11) / 4 memory file 갱신 / cargo build clean

### 6.4 검증 (3-way verify, D-105)
| check | 결과 |
|---|---|
| `cargo build --manifest-path myharness/Cargo.toml -p myharness-tools --release` | ✅ Finished `release` profile |
| `cargo clippy --manifest-path myharness/Cargo.toml --workspace --all-targets -- -D warnings` | ✅ 0 warning (no `#[allow]` 추가, no unsafe) |
| `cargo test --manifest-path myharness/Cargo.toml -p myharness-tools --lib` | ✅ **72 passed** (62 baseline + 10 new) |
| `cargo test --manifest-path myharness/Cargo.toml --workspace --lib` | ✅ **446 passed** + 0 failed + 2 ignored (D-104 baseline 436 → +10, 회귀 0) |

dual-push: upstream (GitHub) + origin (Gitea)

## 7. cumulative 결정

- D-104: 51 → **52**
- D-105: 52 → **53** (총 누적)
- main = `5e39f5e` (D-105 commit, D-104 commit `43bc908` 의 자식)

## 8. D-105 작업 상세 (Edit v2 line_anchored mode)

### 8.1 why
v1 edit.rs 의 `old_string` / `new_string` / `replace_all` 패러다임 한계:
- LLM 이 old_string 정확히 복사 필요 (typo 1자 → fail)
- 1000+ lines 파일의 멀티 edit 에서 line N anchor 어려움
- stale anchor 감지 불가 (다른 agent / tool / user 가 그 사이 파일 변경했는지 모름)

D-104 가 Read v2 (LINE:TEXT + 4-hex content_hash) 를 emit 하면, LLM 이 line N 으로 anchor 가능 + content_hash 를 expected_hash 로 즉시 사용 가능 → D-105 로 직접 연결.

### 8.2 implementation
**`myharness/crates/tools/src/edit.rs` (132 → 658 lines, +537 -11)**:

1. **`LineAnchoredEdit` struct** (private, serde Deserialize):
   - `start_line: usize` (1-indexed inclusive)
   - `end_line: usize` (1-indexed inclusive)
   - `expected_hash: String` (4-hex uppercase, Read v2 metadata `content_hash` 와 동일 형식)
   - `replacement: String` (multi-line 가능, `\n` 으로 split)

2. **`apply_line_replacement` helper** (private fn, `Result<String, String>`):
   - Validation: `start_line_1 >= 1` / `end_line_1 >= start_line_1` / `end_line_1 <= total_lines` (clear error msg)
   - Algorithm: `lines[..start-1] + repl_lines + lines[end..]` → `Vec<&str>` concat → `join('\n')` → trailing `\n` 보존
   - Empty replacement = 0 lines (delete semantics)
   - Non-empty replacement = `split('\n').collect()` (multi-line preserve)

3. **`EditTool::execute_line_anchored`** (private async method):
   - 순서: read → `compute_content_hash` → stale anchor reject → range check → `apply_line_replacement` → write → `compute_content_hash` 재계산 → metadata
   - Errors via `Err(ToolError::InvalidInput(...))` (기존 "old_string not found" 패턴 정합)
   - Stale anchor error message: `"stale anchor: file modified; re-read with \`Read\` tool (current hash XXX, expected YYY)"` (spec prefix + suffix 추가)

4. **`EditTool::execute` dispatch**:
   - `if input.get("line_anchored").is_some() { return self.execute_line_anchored(...); }` 위치: file_path parse 직후
   - **Legacy old_string branch (line 109-258)는 byte-identical** 유지
   - file_path `&str → String` clone 은 borrow checker 강제 (의미 변화 없음)

5. **변경 없음**: `lib.rs` / `content_hash.rs` / `read.rs` / `error.rs` / `tool.rs` / `Cargo.toml` 모두 그대로 (LineAnchoredEdit internal, sha2 기존 dep 재사용)

### 8.3 spec deviation (1건, commit message 에 정직 명시)
- Spec §5.2: `"stale anchor: file modified; re-read with \`Read\` tool"`
- 구현: `"stale anchor: file modified; re-read with \`Read\` tool (current hash XXX, expected YYY)"`
- 이유: LLM 이 drift 규모 (1 char vs full rewrite) 즉시 판별 가능
- Spec prefix 는 그대로 유지

### 8.4 metadata 형태
```json
{
  "path": "/abs/path",
  "mode": "line_anchored",
  "start_line": 2,
  "end_line": 4,
  "replaced_lines": 3,
  "old_hash": "A1B2",
  "new_hash": "C3D4"
}
```

### 8.5 test (10건)

**`#[tokio::test]` (8)**:
1. `test_edit_line_anchored_happy_path` — 6-line file, replace 2..=4 → content + metadata 검증
2. `test_edit_line_anchored_stale_anchor` — hash 캡처 → 외부 write → Err(InvalidInput("stale anchor")) + 파일 unchanged
3. `test_edit_line_anchored_out_of_range` — end_line=99 vs 3-line file → Err + 파일 unchanged
4. `test_edit_line_anchored_invalid_range` — start=5 > end=2 → Err + msg에 start_line/end_line 언급
5. `test_edit_line_anchored_single_line` — start==end, replaced_lines=1
6. `test_edit_line_anchored_preserve_trailing_newline` — `read_back.ends_with('\n')` 명시 검증
7. `test_edit_line_anchored_entire_file` — start=1, end=total → replacement is whole file
8. `test_edit_line_anchored_multiline_replacement` — `"X\nY\nZ"` → 3 lines split

**`#[test]` (1, sync unit)**:
9. `test_apply_line_replacement_unit` — direct helper: single-line / multi-line / multi-line range / entire-file / no-trailing-nl / empty replacement=delete / start=0 reject / start>end reject / end>total reject

**Regression (1)**:
10. `test_edit_old_mode_still_works` — 기존 old_string/new_string/replace_all → `replacements: 2`, NO `mode:line_anchored`

### 8.6 검증 결과

| check | 결과 |
|---|---|
| `cargo build --manifest-path myharness/Cargo.toml -p myharness-tools --release` | ✅ Finished `release` profile |
| `cargo clippy --manifest-path myharness/Cargo.toml --workspace --all-targets -- -D warnings` | ✅ 0 warning (no `#[allow]` 추가, no `unsafe`) |
| `cargo test --manifest-path myharness/Cargo.toml -p myharness-tools --lib` | ✅ **72 passed** / 0 failed (62 baseline + 10 new) |
| `cargo test --manifest-path myharness/Cargo.toml --workspace --lib` | ✅ **446 passed** + 0 failed + 2 ignored (D-104 baseline 436 → +10, 회귀 0) |

### 8.7 next 1순위 (yklee 결정 대기)

| 후보 | scope | effort |
|---|---|---|
| (a) D-106+ tree-sitter 도입 | v1.5+, `replace block N` (function/class/impl 전체 rewrite) | dep weight ↑ (tree-sitter 0.25 + tree-sitter-rust 0.23 + language pack) |
| (b) D-106+ pure insert/delete | `insert before/after N`, multi-section atomic 보장 | manual parser (Lark 도입 ❌, 단순 Rust split-and-validate) |
| (c) A-proper native tool calling | OpenAI/Anthropic wire format (CompletionRequest::tools + CompletionResponse::tool_calls) | v1.5+, 2-3 commit |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read + prompt 보강 | 1 commit |
| (e) TASK-002 도메인 명령 | 서버/환경 3-도메인 명령 가이드 | yklee 인프라 정보 의존 |
| (f) MiniMax OAuth real flow | console device grant 활성화 대기 | yklee trigger |
| (g) TUI shell + interactive mode | binary `myharness` 실행 + LoopRunner 통합 | smoke test |
| (h) 추가 안정화 | cargo tree -d hygiene / 41GB target dir cleanup | 1 commit |
| (i) TASK-005-2 v2.0 Plugin 4-계층 후속 (Sub-task 2/3/4) | auto memory는 D-98/D-99 완료, provider-auto-config/marketplace/installer 잔여 | 큰 사이클 |

### 8.8 외부 blocker (유지)

- **TASK-002**: 도메인별 명령 가이드 (yklee 인프라 정보 = SSH 호스트 / Brewfile / dotfiles / asdf/rtx)
- **MiniMax OAuth real flow**: console device grant 활성화 대기 (client_id 78257093-7e40-4613-99e0-527b14b39113 통합 완료, mock e2e 검증 완료)
- **OpenAI/Google OAuth**: client_id 등록 대기
- **Gitea PAT 회전**: 이전 세션 노출 가능성

---

## 9. 세션 종료 (2026-07-01)

- **상태**: D-105 Edit v2 line_anchored mode 완료 + commit + dual-push 완료. **oh-my-pi Hashline 점진 차용 2차 cycle 종료**.
- **main**: `d52ef78` (코드 commit `5e39f5e` + 메모리 commit `d52ef78`, **단일 push 2 commit** — Option B pattern).
- **누적 결정**: **53** (D-22~D-38 컨셉 + D-42~D-84 + D-96~D-105).
- **build/test 상태**: `cargo test --workspace --lib` = **446 pass + 0 fail + 2 ignored** / `cargo clippy --workspace --all-targets -- -D warnings` = **0 warning** / `cargo build --release -p myharness-tools` = clean.
- **Hashline 점진 차용 누적 (Hybrid 안, D-100 9-area roadmap)**: **5 area 점유** (LINE:TEXT Read + content hash + replace N..M). 잔여 4 area = tree-sitter `replace block` 2 + Lark multi-section parser 1 + SnapshotStore 1 (v2).
- **다음 세션 시작 시 yklee 결정 옵션**: §8.7 (a)~(i) 그대로 유효.
- **외부 blocker 유지**: §8.8 그대로 유효.
- **다음 세션 시작 진입점**: 본 파일 (`backlog/2026-07-01.md`) + `state.json` + `session_handoff.md` + `work_backlog.md` (4 file 모두 2026-07-01 동기화 완료). main = `d52ef78`. D-106+ 진입 결정 대기.
- **dual-push 상태**: Gitea (origin) `43bc908..d52ef78` ✅ / GitHub (upstream) `43bc908..d52ef78` ✅.

---

## 10. 세션 D (D-106 진입 시도 + sandbox blocker)

### 10.1 why
yklee 결정 = (a) D-106+ tree-sitter 도입. oh-my-pi `replace block N` 의미론 차용 (function/if/loop/class body 전체 rewrite, tree-sitter 가 closing line resolve → tight range 위험 0).

### 10.2 설계 결정 (구현 시도 단계)
- **언어 범위**: Rust only v1.5 (LLM use case 80%+, dep weight ↓). 향후 `.ts/.py/.go` 확장 가능하도록 트랙만 남김.
- **입력 스키마**: `block_anchored` 모드 추가 — `{ start_line, expected_hash, replacement }`. `line_anchored` 와 dispatch (한쪽이 존재하면 해당 path).
- **resolve 알고리즘**:
  1. file read → `current_content`
  2. `compute_content_hash` → stale anchor check (D-105 동일 gate)
  3. `tree_sitter::Parser` + `tree_sitter_rust::LANGUAGE` → parse Rust AST
  4. root node 부터 line N 을 포함하는 가장 큰 node 탐색
  5. 그 node 의 end_line 까지 `apply_line_replacement` 와 동일하게 swap
  6. write + new_hash 재계산 + metadata `{mode: "block_anchored", start_line, end_line, replaced_lines, old_hash, new_hash, node_kind, node_text: <opaque>}`
- **non-rust**: 확장자 != `.rs` 일 때 `Err("block_anchored: tree-sitter grammar for <ext> not yet supported; use line_anchored for non-Rust files")` (fail-fast, future-proof).
- **decorator/doc-comment 처리**: oh-my-pi spec 그대로 — N 은 block opener line (decorator 첫 line 도 OK, Python `@dec`+`def` 1 block). Rust `///` doc-comment 는 별도 node 이므로 line_anchored 권장.

### 10.3 dep 추가 시도
- `myharness/Cargo.toml` workspace dep: `tree-sitter-rust = "0.23"` 추가 ✅ (sed)
- `myharness/crates/tools/Cargo.toml`: `tree-sitter = { workspace = true }` + `tree-sitter-rust = { workspace = true }` 추가 ✅ (sed)
- `cargo build -p myharness-tools` 시도 → **`Could not resolve host: index.crates.io`** (sandbox 의 network 차단)
- cargo fetch 단계에서 DNS 실패 → **빌드 검증 불가**
- 모든 변경 `git checkout ... && sed -d` 로 working tree 복원 완료. main = `a9b5a63` clean.

### 10.4 blocker 분석
- **network**: `sandbox_mode = workspace-write`, `index.crates.io` DNS sandbox 밖
- **Cargo.lock**: tree-sitter 0.26 / tree-sitter-rust 0.23 둘 다 미존재 → 새 dep 2개
- **registry 미러**: `.cargo/config.toml` 없음, nexus 사설 mirror config 없음 → fetch path = crates.io only
- **에스컬레이션 옵션**: `cargo build` 만이라도 sandbox 밖에서 실행 가능 (require_escalated + `["cargo", "fetch"]` / `["cargo", "build", "-p", "myharness-tools"]` prefix_rule). 단, registry 182MB tree-sitter 0.23 fetch 가 1-2분 소요.

### 10.5 메모리 결정
- **D-106 진행 보류** (코드 0, 메모리 only 갱신). 다음 세션 시작 시 yklee 가 옵션 선택:
  - **(A) 에스컬레이션 허용** → tree-sitter-rust fetch + build + tree-sitter resolve 구현 (1-2 commit, D-106 完)
  - **(B) Cargo.lock 사전 fetch** → 외부에서 `cargo fetch` 만 1회 실행 (의미: dep tree 먼저 확정, 다음 세션에서 sandbox 안 빌드 가능)
  - **(C) D-106 deprioritize** → (a) skip, (b) pure insert/delete 또는 (c) A-proper native tool calling 으로 pivot (dep 0)
  - **(D) nexus mirror 설정** → `.cargo/config.toml` 에 사설 mirror 등록 후 진행 (yklee 인프라 정보 의존)

### 10.6 변경 file (이번 cycle, 메모리 only)
- `ai-workflow/memory/backlog/2026-07-01.md` (본 파일, §10 추가)
- `ai-workflow/memory/state.json` (current_baseline / current_axis / next_actions 갱신 예정)
- `ai-workflow/memory/session_handoff.md` (D-106 시도 + blocker 명시)
- `ai-workflow/memory/work_backlog.md` (D-106 in_progress → blocked, blocker reason)

### 10.7 누적 결정
변동 없음. 53 유지 (D-106 미완 = +0).

### 10.8 next 1순위 (yklee 결정)
| 옵션 | scope | sandbox 가능? |
|---|---|---|
| (A) 에스컬레이션 + tree-sitter fetch/build | dep 2개 fetch + tree-sitter resolve 구현 | ❌ network |
| (B) 외부 cargo fetch 1회 | Cargo.lock 만 확정 | ❌ network (A 와 동일) |
| (C) D-106 skip → (b) or (c) pivot | dep 0 | ✅ |
| (D) nexus mirror | .cargo/config.toml 추가 | yklee 정보 |

---

## 11. 세션 D-107 (pure_edit mode — multi-section insert/delete)

### 11.1 why
yklee 결정 = (b) D-106+ pure insert/delete. oh-my-pi `insert before/after N` + `delete N..M` + `insert head/tail` 의미론. D-105 line_anchored 가 "기존 line N..M 통째 교체" 만 가능했다면, D-107 은 **순수 추가/삭제** 를 multi-section 으로 한 patch 에 묶음.

### 11.2 설계
- 입력 스키마 (D-105 와 통일, stale-anchor gate 동일):
  ```json
  {
    "file_path": "...",
    "pure_edit": {
      "expected_hash": "A1B2",
      "insertions": [
        {"op": "insert_before", "line": 5, "content": "..."},
        {"op": "insert_after",  "line": 7, "content": "..."},
        {"op": "insert_head",   "content": "..."},
        {"op": "insert_tail",   "content": "..."},
        {"op": "insert_after_block", "line": 3, "content": "..."}
      ],
      "deletions": [
        {"start_line": 5, "end_line": 7},
        {"start_line": 9, "end_line": 9}
      ]
    }
  }
  ```
- **multi-section atomic**: 모든 op 를 line-DESCENDING 으로 정렬 후 순차 적용. high anchor op 가 low anchor op 의 anchor 를 shift 시키지 않음. priority: Delete(0) > After(1) > Before(2) > Head|Tail(3).
- **stale-anchor gate**: v1.5 safe-by-default — `expected_hash` 필수 (없으면 reject). pure_edit 는 file mutation 만 하므로 gate 가 critical.
- **insert_after_block**: D-106 `resolve_block_span` 재사용. Rust only (D-106 v1.5 scope 정합).
- **non-rust with insert_after_block**: explicit reject (`tree-sitter grammar for .{ext} not yet supported`).
- **return metadata**: `{mode: "pure_edit", applied: [{op, line|start_line, lines_added|lines_removed}], old_hash, new_hash}`.

### 11.3 변경
- `myharness/crates/tools/src/edit.rs`: 658 → 1220 lines, +562
  - PureEdit + PureInsertion (5 variants, serde rename) + PureDeletion struct
  - PendingOp + OpKind (internal sort representation)
  - apply_insert_before / apply_insert_after helper (split_inclusive('\n'), empty = no-op)
  - EditTool::execute_pure private async method
  - EditTool::execute dispatch: pure_edit 분기

### 11.4 test 추가 (10)
**unit (2)**: `test_apply_insert_before_basic` / `test_apply_insert_after_basic`
**tokio (8)**: `test_pure_edit_insert_before` / `test_pure_edit_insert_after_head_tail` / `test_pure_edit_delete_single_line` / `test_pure_edit_delete_range` / `test_pure_edit_multi_section_atomic` (insert_before line 2 + delete line 4, line-descending sort 로 atomic) / `test_pure_edit_stale_anchor` / `test_pure_edit_empty_rejected` / `test_pure_edit_missing_hash_rejected`

### 11.5 검증
| check | 결과 |
|---|---|
| `cargo build -p myharness-tools` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning (no `#[allow]` 추가, no unsafe) |
| `cargo test -p myharness-tools --lib` | ✅ **93 pass** / 0 fail (D-106 baseline 83 → +10) |
| `cargo test --workspace --lib` | ✅ **467 pass** + 0 fail + 2 ignored (D-106 baseline 457 → +10, 회귀 0) |

### 11.6 누적 결정
53 → **54** (D-106, D-107).

### 11.7 main / push
- main = `cda6330` (코드 1 commit). 다음 메모리 commit 으로 단일 push.
- 누적 결정: 53 (D-106) → **54** (D-107 추가).

### 11.8 Hashline 점진 차용 누적 (D-100 9-area roadmap, Hybrid 안)
**7 area 점유** (D-104 + D-105 + D-106 + D-107):
- LINE:TEXT Read format
- content hash tag (4-hex)
- `replace N..M` (concrete lines)
- `replace block N` (tree-sitter Rust)
- `insert before/after N` + `insert head/tail`
- `delete N..M`
- multi-section atomic 보장 (line-descending sort)

**잔여 2 area**:
- Lark multi-section parser (단일 patch 에서 multiple file × multiple section) — v1.5+ (D-108+)
- SnapshotStore + 3-way merge recovery — v2 (Plugin Sub-task 4 + hindsight memory 결합 시점)

### 11.9 다음 옵션
| 후보 | scope | effort |
|---|---|---|
| (c) A-proper native tool calling | OpenAI/Anthropic wire format (CompletionRequest::tools + tool_calls response) | v1.5+, 2-3 commit |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read + prompt 보강 | 1 commit |
| (e) TASK-002 도메인 명령 | 서버/환경 3-도메인 (yklee 인프라 정보 의존) | 1 commit |
| (f) MiniMax OAuth real flow | console device grant 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary `myharness` 실행 + LoopRunner 통합 | smoke test |
| (h) 추가 안정화 | cargo tree -d hygiene / 41GB target dir cleanup | 1 commit |
| (i) D-108+ Lark multi-section parser | cross-file multi-section patch | v1.5+ 큰 사이클 |
| (j) D-108+ insert_before_block / replace_block 와의 통합 | `replace block N` + `insert before block N` | 1-2 commit |

---

## 12. 세션 D-108 (A-proper native tool calling)

### 12.1 why
yklee 결정 = (c) A-proper native tool calling. D-100 A-min 의 한계: LLM 이 ```tool_call``` 코드블록 안에 JSON emit, 파싱 오류 가능 / multi-call 단일 round 어려움. OpenAI wire format (`tools: [{type: function, function: {name, description, parameters}}]` + `choices[0].message.tool_calls: [{id, function: {name, arguments (string JSON)}}]`) 와 Anthropic wire format (`content: [{type: tool_use, id, name, input}]`) 표준화. v1.5+ 진입.

### 12.2 설계
- `ToolSpec` (name + description + input_schema) — request 에 실어 보냄
- `ToolCall` (id + name + arguments JSON) — response 에서 받음
- `CompletionRequest::tools: Vec<ToolSpec>` (default empty)
- `CompletionResponse::tool_calls: Vec<ToolCall>` (default empty)
- **dispatch 우선순위**: native (r.tool_calls.is_empty() == false) → text-based (D-100 A-min) → final text
- **D-102 dedup (canonical signature)**: native path 에도 적용
- **D-108 v1 scope**:
  - (1) `tools` / `tool_calls` field 추가 ✅
  - (2) MockClient::ToolCalls variant (native emit 시뮬레이션) ✅
  - (3) orchestrator native path + dispatch ✅
  - (4) Tool trait 에 description / input_schema method (v1.5 = name only)
  - (5) OpenAI-compat / Anthropic wire format 직접 작성 (D-108 follow-up, rig-core 0.38.1 의 CompletionRequest 에 tools builder 없음)

### 12.3 변경 (8 file / +305 / -1)
- myharness/crates/llm/src/client.rs: +ToolSpec + +ToolCall + CompletionRequest::tools + CompletionResponse::tool_calls
- myharness/crates/llm/src/client_mock.rs: +MockResponse::ToolCalls variant
- myharness/crates/llm/src/client_anthropic.rs / client_gemini.rs / client_openai_compat.rs: tool_calls: Vec::new() literal field
- myharness/crates/tui/src/orchestrator.rs: +tool_specs_for helper + native dispatch path + D-102 dedup native 적용
- myharness/crates/compression/src/summarizer.rs + myharness/crates/cli/src/refreshing_client.rs: tools: Vec::new() literal

### 12.4 test 추가 (6)
**orchestrator (4 tokio)**: `run_with_native_tool_call_dispatches_and_continues` / `run_with_native_tool_call_request_includes_tools` (registry 6 tool name 모두 request 에 포함) / `run_with_native_tool_calls_takes_precedence_over_text` (BOTH 있으면 native win) / `run_with_native_repeated_tool_call_breaks_loop` (D-102 dedup native path)
**unit (2)**: `tool_specs_for_empty_registry` / `tool_specs_for_default_registry` (6 tool spec 생성)

### 12.5 검증
| check | 결과 |
|---|---|
| `cargo build -p myharness-tui` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning (no `#[allow]`, no unsafe) |
| `cargo test --workspace --lib` | ✅ **473 pass** + 0 fail + 2 ignored (D-107 baseline 467 → +6, 회귀 0) |

### 12.6 누적 결정
54 → **55** (D-108 추가).

### 12.7 main / push
- main = `0771228` (코드 1 commit) + 메모리 commit 단일 push.

### 12.8 next 1순위 (D-108 follow-up)
| 후보 | scope | effort |
|---|---|---|
| (A) OpenAI-compat wire format 직접 | reqwest 로 /v1/chat/completions 직접 호출, tools wire 변환, response tool_calls parse | 1-2 commit |
| (B) Anthropic wire format | content block tool_use parse (Anthropic API) | 1-2 commit |
| (C) Tool trait description / input_schema | 각 tool 의 description + JSON Schema 노출 | 1 commit |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read | 1 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before block N` | 1-2 commit |

---

## 13. 세션 D-108 follow-up (OpenAI-compat wire format native tool calling)

### 13.1 why
yklee 결정 = (A) D-108 follow-up: OpenAI-compat wire format 직접 구현. rig-core 0.38.1 의 CompletionsClient builder 가 tools field 미노출. reqwest 로 /v1/chat/completions 직접 호출 → MiniMax / DeepSeek / Ollama / llama.cpp 등 OpenAI-compat provider 전부 native tool calling 가능. 기존 rig-core path 는 plain text request 에서 byte-identical (zero behavior change).

### 13.2 설계
- `OpenAiCompatProvider::complete()` 진입 시 `!req.tools.is_empty()` 이면 `complete_wire_format()` 으로 dispatch (authoritative, rig-core path skip). tools 비어있으면 기존 rig-core path.
- `complete_wire_format()`: reqwest POST `{base_url}/chat/completions` + Bearer auth + JSON body. HTTP status check. parse_chat_response().
- `build_chat_payload()`: OpenAI request shape — model + messages + max_tokens + temperature + tools[] (function type) + stream=false. tool message 의 `tool_call_id` 는 `Message::tool(name: "call_id:xxx")` prefix 로 받음.
- `parse_chat_response()`: OpenAI response shape — `choices[0].message.tool_calls: [{id, type, function: {name, arguments (string JSON)}}]` → `Vec<ToolCall>`. arguments string → `serde_json::Value` (parse fail 시 `LlmError::ProviderCall` with offending snippet).
- 6 private struct (ChatResponse / ChatChoice / ChatMessage / ChatToolCall / ChatToolCallFunction / ChatUsage) + `#[allow(dead_code)]` on forward-compat field (call_type, extra flatten).
- **scope**: OpenAI-compat only. Anthropic wire format (content block tool_use) 은 follow-up.

### 13.3 변경 (1 file / +396 / -1)
- myharness/crates/llm/src/client_openai_compat.rs:
  - +api_key field (wire format path 가 authenticate)
  - +complete_wire_format inherent method (impl OpenAiCompatProvider)
  - +build_chat_payload + ChatResponse / ChatChoice / ChatMessage / ChatToolCall / ChatToolCallFunction / ChatUsage
  - +parse_chat_response

### 13.4 test 추가 (8)
**build_chat_payload (3)**: `build_chat_payload_includes_tools_and_messages` (full payload shape — model/messages/max_tokens/tools[]/description/parameters 모두 검증) / `build_chat_payload_omits_tools_when_empty` (tools empty 면 field skip) / `build_chat_payload_tool_message_carries_call_id` (tool message 의 `tool_call_id` 라우팅)
**parse_chat_response (5)**: `parse_chat_response_plain_text` (no tool_calls) / `parse_chat_response_with_tool_calls` (2 calls, args string → Value) / `parse_chat_response_invalid_tool_args_yields_error` / `parse_chat_response_no_choices_yields_error` / `parse_chat_response_mixed_text_and_tool_calls` (Anthropic-via-OpenAI-compat 패턴)

### 13.5 검증
| check | 결과 |
|---|---|
| `cargo build -p myharness-llm` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning (no `#[allow]` 추가, 단 forward-compat field 3건만 `#[allow(dead_code)]`) |
| `cargo test -p myharness-llm --lib` | ✅ **128 pass** / 0 fail / 2 ignored (D-108 baseline 120 → +8) |
| `cargo test --workspace --lib` | ✅ **481 pass** + 0 fail + 2 ignored (D-108 baseline 473 → +8, 회귀 0) |

### 13.6 누적 결정
55 → **56** (D-108 follow-up 추가).

### 13.7 main / push
- main = `23a205a` (코드 1 commit) + 메모리 commit 단일 push.

### 13.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) D-108 follow-up Anthropic wire format | content block tool_use parse, Anthropic API 직접 호출 | 1-2 commit |
| (C) Tool trait description + input_schema | 각 tool 의 description + JSON Schema 노출 (현재 v1.5 = name only minimal spec) | 1 commit |
| (D) Real MiniMax native E2E | MINIMAX_API_KEY 주입 시 wire format 으로 native tool call E2E | smoke |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read | 1 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before block N` | 1-2 commit |

## 14. 세션 — D-109 Tool trait description + input_schema

**옵션 C** (yklee 결정). D-108 follow-up (OpenAI-compat wire format native tool calling) 의 wire format 보강. 현재 `Tool` trait 은 `name()` 만 노출 → wire format 의 `function.description` + `function.parameters` 가 빈 string/empty object 로 emit. LLM 이 tool 분기 시 정보 부족. `description()` + `input_schema()` default method 추가 + 6 tool impl override + tui/orchestrator `tool_specs_for` 가 live 메타데이터 사용하도록 개선.

### 14.1 결정
- **trait 변경** — `description()` (`&'static str` default `""`) + `input_schema()` (`serde_json::Value` default `{"type":"object","properties":{}}`) default method 추가. 기존 impl 깨지지 않음 (override 만 새 tool 이 추가).
- **6 tool override** — read / write / edit / bash / glob / grep 각 impl 에 human-readable description + JSON Schema (required + properties + enum/minimum) 추가.
- **Edit tool schema** — 3 modes (line_anchored D-105 / block_anchored D-106 / pure_edit D-107) + classic old_string/new_string 모두 properties 에 surface. `required: ["file_path"]` 만.
- **tui 변경** — `tool_specs_for` 가 `reg.get(name) → Some(Arc<dyn Tool>)` 받아 `tool.description()` + `tool.input_schema()` 호출. 기존 name-only fallback 도 안전망 유지.
- **wire format 변경 불요** — D-108 follow-up 의 `client_openai_compat.rs::build_chat_payload` 가 이미 `t.description` + `t.input_schema` 를 `function.description` + `function.parameters` 로 emit. tui 에서 진짜 데이터를 채워주기만 하면 wire format 이 자동으로 풍부해짐.

### 14.2 파일 / 변경
| file | 변경 |
|---|---|
| `myharness/crates/tools/src/tool.rs` | `description()` + `input_schema()` default method 추가 |
| `myharness/crates/tools/src/read.rs` | override (file_path req + offset/limit/format opt) |
| `myharness/crates/tools/src/write.rs` | override (file_path + content req) |
| `myharness/crates/tools/src/edit.rs` | override (3 modes + old_string/new_string) |
| `myharness/crates/tools/src/bash.rs` | override (command req + timeout_ms opt) |
| `myharness/crates/tools/src/glob_.rs` | override (pattern req + path opt) |
| `myharness/crates/tools/src/grep.rs` | override (pattern req + path/include opt) |
| `myharness/crates/tools/src/lib.rs` | +7 test (description + schema 검증) |
| `myharness/crates/tui/src/orchestrator.rs` | `tool_specs_for` 개선 + +1 test |

### 14.3 test 추가 (총 +8)
- `tools::tests::d109_all_default_tools_declare_description_and_schema` — 6 tool 모두 non-empty description + required non-empty
- `tools::tests::d109_read_tool_schema_is_well_formed` — required = ["file_path"], properties file_path/offset/limit/format
- `tools::tests::d109_write_tool_schema_requires_content` — required ⊇ ["file_path", "content"]
- `tools::tests::d109_edit_tool_schema_includes_modes` — properties ⊇ old_string/new_string/line_anchored/block_anchored/pure_edit
- `tools::tests::d109_bash_tool_schema_requires_command` — required = ["command"], properties timeout_ms
- `tools::tests::d109_glob_tool_schema_requires_pattern` — required = ["pattern"]
- `tools::tests::d109_grep_tool_schema_requires_pattern_and_supports_include` — required = ["pattern"], properties include
- `tui::tests::d109_tool_specs_carry_description_and_schema` — `tool_specs_for` 가 description + schema 모두 surface

### 14.4 검증
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning |
| `cargo test --workspace --lib` | ✅ **489 pass** + 0 fail + 2 ignored (D-108 follow-up baseline 481 → +8, 회귀 0) |

### 14.5 누적 결정
56 → **57** (D-109 추가).

### 14.6 main / push
- main = D-109 commit (코드 + 메모리 단일 push 2 commit).

### 14.7 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) D-108 follow-up Anthropic wire format | content tool_use block parse, Anthropic API 직접 호출 | 1-2 commit |
| (D) Real MiniMax native E2E | MINIMAX_API_KEY 주입 시 D-108 follow-up + D-109 의 wire format 으로 native tool call E2E | smoke |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read | 1 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before block N` | 1-2 commit |

## 15. 세션 — D-110 Real MiniMax native E2E test (옵션 D)

**옵션 D** (yklee 결정). D-108 follow-up (OpenAI-compat wire format) + D-109 (Tool description + input_schema) 가 실제 MiniMax API 까지 도달하는지 검증하는 `#[ignore]` test 추가. **이 세션은 `MINIMAX_API_KEY` env 부재로 실제 실행은 다음 세션에 yklee 가 주입 후**.

### 15.1 결정
- **test-only** 변경. 기존 `minimax_real_api_smoke` (plain text) 옆에 `minimax_real_native_tool_call` (native tool calling) 추가.
- 검증 5단계: (1) `complete_wire_format` 분기 발동 (tools 비어있지 않으면 wire format path), (2) payload 가 D-109 description + input_schema 정확히 실어 나르는지 dump, (3) 응답 `tool_calls` 비어있지 않음, (4) `tool_calls[0].name == "Read"`, (5) arguments 에 `file_path` 존재.
- API key 부재 시 `eprintln!` + `return` → CI 안전.

### 15.2 파일 / 변경
| file | 변경 |
|---|---|
| `myharness/crates/llm/src/client_openai_compat.rs` | +1 `#[tokio::test] #[ignore]` (`minimax_real_native_tool_call`, ~75 lines) |

### 15.3 실행 명령
```bash
export MINIMAX_API_KEY=...
cargo test -p myharness-llm minimax_real_native_tool_call -- --ignored --nocapture
```

### 15.4 검증 (이 세션 = test-only, ignored 미실행)
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning |
| `cargo test --workspace --lib` | ✅ **490 pass** + 0 fail + 3 ignored (D-109 baseline 489 → +1 ignored, 회귀 0) |

### 15.5 누적 결정
57 → **58** (D-110 추가).

### 15.6 main / push
- main = D-110 commit (코드 + 메모리 단일 push 2 commit).

### 15.7 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (D 실행) | `export MINIMAX_API_KEY=...` + 위 명령 → 결과 dump → 옵션 (B) 또는 backlog | smoke (1-2 min) |
| (B) D-108 follow-up Anthropic wire format | content tool_use block parse, Anthropic API 직접 호출. **test-only 가능** (mock server, API key 불요) | 1-2 commit |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read | 1 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before block N` | 1-2 commit |

## 16. 세션 — D-111 D-110 E2E test real network verification (옵션 D 실행)

**옵션 D 실행** (yklee 결정). D-110 의 ignored test 가 실제 MiniMax API 에 도달하는지 verification cycle. **memory-only 변경**, code 0 / test 0.

### 16.1 실행
```bash
export MINIMAX_API_KEY=sk-cp-... # yklee 가 주입
cargo test -p myharness-llm --manifest-path myharness/Cargo.toml \
  minimax_real_native_tool_call -- --ignored --nocapture
unset MINIMAX_API_KEY
```

### 16.2 결과 (실제 output)
```
MiniMax native response: model=MiniMax-M3 content="<think>\nThe user wants me to read the file at /tmp/ping.txt. I must use the Read tool and only emit the tool call, no prose.\n</think>\n\n</think>" tool_calls=1
  call[0]: id=call_019f1b96d53f7f21b04dca15 name=Read args={"file_path":"/tmp/ping.txt"}
test client_openai_compat::tests::minimax_real_native_tool_call ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 130 filtered out; finished in 2.83s
```

### 16.3 검증 단계 5가지 모두 PASS
| # | 검증 | 결과 |
|---|---|---|
| 1 | `complete_wire_format` 분기 발동 (real HTTP POST `https://api.minimax.io/v1/chat/completions` 도달) | ✅ PASS (HTTP 200 + 1.5s round-trip) |
| 2 | payload 가 D-109 description + input_schema 정확히 전달 (LLM 이 spec 이해) | ✅ PASS (LLM 이 tool spec 받아 Read 선택) |
| 3 | `tool_calls` 1개 emit (non-empty) | ✅ PASS (`tool_calls=1`) |
| 4 | `tool_calls[0].name == "Read"` (LLM 이 올바른 tool 선택) | ✅ PASS (`name=Read`) |
| 5 | `arguments.file_path == "/tmp/ping.txt"` (LLM 이 prompt 의 path 정확히 추출) | ✅ PASS (`args={"file_path":"/tmp/ping.txt"}`) |

### 16.4 의미
D-108 follow-up (OpenAI-compat wire format native tool calling) + D-109 (Tool description + input_schema) 의 full chain 이 **real MiniMax API 와 end-to-end 로 verified**. 

→ MiniMax / DeepSeek / Ollama / llama.cpp 등 모든 OpenAI-compat provider 에 대해 native tool calling 이 동작 (D-110 의 ignored test 가 그대로 재사용 가능, base_url / api_key 만 다르게).

### 16.5 누적 결정
58 → **59** (D-111 추가).

### 16.6 main / push
- main = D-111 commit (메모리 only, hash push 후 확정). code 변경 0.

### 16.7 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) D-108 follow-up Anthropic wire format | content tool_use block parse, Anthropic API 직접 호출. **test-only 가능** (mock server, API key 불요) | 1-2 commit |
| (d) D-100 한계 follow-up | 큰 file 1000+ lines chunked Read | 1 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before block N` | 1-2 commit |

## 17. 세션 — D-112 Read tool auto-truncation + has_more/next_offset hint (옵션 d)

**yklee 결정**: 옵션 (d) D-100 한계 follow-up. Read tool 의 1MB hard cap 은 유지하면서, D-103 의 prompt-only 강제였던 chunked Read 를 tool layer 로 enforce. code-only 변경.

### 17.1 why
D-103 의 한계 = "1000+ lines 진짜 큰 파일은 chunked Read 권고" 가 prompt 수준에 그침. LLM 이 prompt 무시 가능. tool 자체가 enforce + truncation hint 를 metadata 로 노출하면 LLM 부담 0.

### 17.2 결정 사항
- `DEFAULT_READ_LINE_LIMIT = 500` — caller 가 `limit` 미지정 시 자동 cap (D-103 prompt "~200 chunk" 의 2.5x 여유, 500 lines 가 LLM context window 의 per-tool-result budget 안에서 안전).
- `MAX_READ_LINE_LIMIT = 5_000` — caller 가 명시한 limit 도 5000 초과 시 clamp (1MB 안에 fit + LLM context 보호).
- `Some(0) | None` → `DEFAULT_READ_LINE_LIMIT` (defensive: schema 의 `minimum:1` 위배 케이스도 안전).
- `has_more = (offset + emitted) < total_lines` — 잘린 경우만 `true`.
- metadata 에 `limit` (실제 적용된 값) + `has_more` (truncated) + `has_more=true` 일 때만 `next_offset` (다음 chunk 의 offset) 노출.
- description + input_schema 의 `limit` description 갱신 — LLM 이 schema 만 보고도 동작 학습.

### 17.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/tools/src/read.rs` | const 2개 (`DEFAULT_READ_LINE_LIMIT` + `MAX_READ_LINE_LIMIT`) + `limit` clamp + `emitted` 재정의 + metadata `limit`/`has_more`/`next_offset` 추가 + description + input_schema `limit` description 갱신 (+47 -20 net) |
| `myharness/crates/tools/src/read.rs` (test) | **4 신규 test** (`test_d112_auto_truncates_large_file` / `test_d112_clamps_excessive_limit` / `test_d112_no_truncation_signals_correctly` / `test_d112_limit_zero_uses_default` + `test_d112_paginated_walk_through_large_file` for end-to-end chunked walk) |

### 17.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** (no `#[allow]` 추가, no unsafe) |
| `cargo test -p myharness-tools --lib` | ✅ **105/105 pass** (기존 9 + 신규 4 = 13 read test 모두 PASS, 회귀 0) |
| `cargo test --workspace --lib --exclude myharness-auth` | ✅ **452/452 pass** (auth 7 fail 은 xdg-open sandbox env issue, pre-existing, D-112 와 무관) |

### 17.5 효과
- LLM 이 1000+ line 파일을 prompt 무시하고 한 번에 요청해도 자동 truncate → large file code review 무한 루프 위험 자동 차단.
- `has_more` / `next_offset` 가 LLM 에게 next chunk 의 정확한 위치 (`offset: 500, limit: 500`) 알려줌.
- D-103 prompt 의 `>500 lines 는 offset+limit` 권고가 tool layer 로 enforce → prompt 변경 시 description 만 sync.
- 1MB cap + 5000 line hard cap 유지로 binary / maliciously large limit 요청 방어.

### 17.6 한계
- 1MB cap 은 그대로 — binary 같은 큰 file 은 여전히 거부 (옵션 a 미포함).
- Lark parser / block-aware insert/replace / Anthropic wire format / TUI shell / 추가 안정화 등 v1.5+ 영역 미착수.

### 17.7 누적 결정
59 → **60** (D-112 추가).

### 17.8 main / push
- main = D-112 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 17.9 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (f) MiniMax OAuth real flow | console 활성화 대기 | yklee trigger |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 18. 세션 — D-113 Real MiniMax OAuth Device flow client-side E2E (옵션 f)

**yklee 결정**: 옵션 f, MiniMax OAuth real flow 검증. console 활성화 대기로 미뤄뒀는데, **이미 endpoint 가 살아있음** (api.minimax.io 도달 + 307 redirect + PKCE 요구 + 정상 응답). `request_code` 의 client 측 진입까지 real network 로 verified.

### 18.1 why
W14 (D-52 follow-up) 에서 `MinimaxDeviceOAuth` 가 Device Authorization Grant 변형으로 구현됐지만, real MiniMax API 와 검증된 적 없음. option f 는 그 갭 해소.

### 18.2 발견 (D-113 핵심)
- `MinimaxDeviceOAuth::code_endpoint()` (https://api.minimax.io/oauth/code) 가 307 redirect
- redirect target = `https://account.minimax.io/oauth2/device/code` (RFC 8628 표준 endpoint)
- PKCE `code_challenge` (S256) 필수 — 미제출 시 HTTP 400 `invalid_request: code_challenge is required`
- 응답: `user_code` (XXXX-XXXX 9자), `verification_uri=https://platform.minimax.io/oauth-authorize?user_code=...&client=OpenClaw`, `interval=3000` (ms), `expired_in=<future epoch ms>`
- Token endpoint: `https://account.minimax.io/oauth2/token` 도달 (fake device_code → `invalid_grant` 정상)
- `client=OpenClaw` 식별자가 verification_uri 에 박혀있음 → client_id 공유 정책 검증 필요 (production endpoint 갱신 시 결정 보류)

### 18.3 결정 사항
- **production endpoint URL 은 그대로 유지** (307 redirect 자동 follow → production 동작 OK). 명시적 변경은 v1.5+ 에서 결정.
- **memory-only 검증** — production code 변경 0.
- D-114 후보 (다음 옵션): production `MinimaxDeviceOAuth` 의 `code_endpoint` / `token_endpoint` 를 `account.minimax.io/oauth2/{device/code,token}` 으로 명시 변경 + `device_flow.rs` 가 PKCE 정확히 emit 하는지 검증 + mock server test 갱신.

### 18.4 변경
| file | 변경 |
|---|---|
| `myharness/crates/auth/src/device_flow.rs` (test) | +1 `#[ignore]` test `minimax_real_device_request_code` (~115 lines) — `request_code` real 호출 + 6단계 assertion + network fail 시 eprintln! + early return |

### 18.5 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-auth --lib` | ✅ **42/42 pass + 1 ignored** (D-113 ignored, baseline 35 → 42, sandbox 외부에서 7개 pass 회복) |
| `cargo test -p myharness-auth minimax_real_device_request_code -- --ignored --nocapture` | ✅ **PASS** (real network) — `user_code=U66F-2Y23 verification_uri=... interval=3000 expired_in=1782879693270` |
| `cargo test --workspace --lib` | ✅ **494 pass + 0 fail + 4 ignored** (D-110 + D-113 = 2 신규 ignored, 회귀 0) |

### 18.6 효과
- Real MiniMax OAuth flow 의 **client 측 진입** (POST /oauth/code → user_code + verification_uri) 가 end-to-end 로 verified.
- D-114 결정 보류의 근거 확보 (endpoint URL, PKCE 요구, 응답 형식, client 식별자 정책 모두 명시).
- `request_code` 가 production `MinimaxDeviceOAuth::from_env()` 그대로 real endpoint 도달 → production code 0 변경.

### 18.7 한계 / 후속
- `poll_token` 의 real 검증은 user 가 browser 에서 인증해야 가능 → 이번 세션 불가.
- production `MinimaxDeviceOAuth` 의 명시적 endpoint 갱신 (D-114) 보류.
- `client=OpenClaw` 식별자 정책 (client_id 공유) 검증 별도 결정.

### 18.8 누적 결정
60 → **61** (D-113 추가).

### 18.9 main / push
- main = D-113 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 18.10 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (f-1) **D-114 production endpoint 갱신** | `MinimaxDeviceOAuth` URL → `account.minimax.io/oauth2/{device/code,token}` + PKCE 검증 + mock server test 갱신 | 1 commit |
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 19. 세션 — D-114 MinimaxDeviceOAuth endpoint URL 갱신 (옵션 f-1)

**yklee 결정**: 옵션 f-1, D-113 의 real-network test 가 발견한 `https://api.minimax.io/oauth/code` 307 redirect → `https://account.minimax.io/oauth2/device/code` (RFC 8628) 를 production 코드에 명시 적용. **scope = (1) endpoint URL only**, 부속 fix 는 deferred.

### 19.1 why
- W14 의 `MinimaxDeviceOAuth` 가 `https://api.minimax.io/oauth/{code,token}` 으로 시작. real API 가 307 redirect 로 새 endpoint 가리킴.
- D-113 의 real-network test 가 redirect target 확인 → 명시적 endpoint 갱신이 production 의도 일치.

### 19.2 결정 사항
- **endpoint URL 갱신** (production 변경):
  - `code_endpoint` = `https://api.minimax.io/oauth/code` → `https://account.minimax.io/oauth2/device/code`
  - `token_endpoint` = `https://api.minimax.io/oauth/token` → `https://account.minimax.io/oauth2/token`
  - `from_env()` 의 default base_url = `https://api.minimax.io` → `https://account.minimax.io` / CN 도 동일 패턴 (`https://account.minimaxi.com`)
  - if-else 분기 단순화 (CN 도 새 URL)
- module doc + D-113 test doc 갱신 (endpoint URL + D-114 follow-up 메모).
- 0 신규 test, 0 mock test 갱신 (mock server 가 endpoint 직접 주입).

### 19.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/auth/src/provider.rs` | `MinimaxDeviceOAuth::from_env()` default base_url + `code_endpoint` + `token_endpoint` 4 string 갱신 + if-else 분기 단순화 + module doc 갱신 |
| `myharness/crates/auth/src/device_flow.rs` | module doc 갱신 (endpoint URL + D-114 follow-up 메모) + D-113 test doc 갱신 (endpoint 히스토리) |

### 19.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-auth --lib` | ✅ **42/42 pass + 1 ignored** (회귀 0, mock server test 도 PASS) |
| `cargo test -p myharness-auth minimax_real_device_request_code -- --ignored --nocapture` | ✅ **PASS** (real network, 1-hop 직접 hit) — `code_endpoint=https://account.minimax.io/oauth2/device/code user_code=NV6S-SR2T interval=3000 expired_in=1782880012212` |
| `cargo test --workspace --lib` | ✅ **494 pass + 0 fail + 4 ignored** (회귀 0) |

### 19.5 효과
- production `MinimaxDeviceOAuth` 가 real MiniMax API 의 canonical endpoint 직접 hit (redirect 0-hop).
- D-113 test 의 URL = production URL = 동일 (의도 일치).
- module doc + test doc 의 endpoint URL 일관성 확보.

### 19.6 한계 / 후속 결정 (D-115/116/117, deferred)
- **D-115**: real `MiniMax` API 의 token 응답은 `status: "success"` 필드 없이 `base_resp.status_code=0` 만 포함. 현재 `poll_token` 는 `status: "success"` 만 accept → real 사용 시 `TokenPoll::Error("unknown status:")` 로 떨어질 위험. (small fix, request_code 와 poll_token 양쪽에 base_resp branch 추가)
- **D-116**: real API 의 `expired_in` / `interval` 은 epoch ms (예: `expired_in=1782880012212`), 우리 `DeviceAuthorization::expired_in: u64` / `interval: u64` 는 초 가정. real flow 의 `OAuthToken::is_expired` 가 1000배 어긋남. `manager.rs` 의 `current_token("minimax")` 도 같이 갱신 필요.
- **D-117 (optional)**: `response_type=code` 파라미터 제거. real spec 에 없음 (real response 가 `state` 만 echo). mock test 호환성 위해 유지.
- **위 3건 모두 real 사용자/테스트가 없으면 production 영향 없음** (mock test 만 영향). D-113 의 real test 가 `request_code` 만 검증하므로 polling token 단계는 미검증.

### 19.7 누적 결정
61 → **62** (D-114 추가).

### 19.8 main / push
- main = D-114 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 19.9 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (f-2) **D-115 base_resp 처리** | real `poll_token` 동작 위해 `base_resp.status_code==0` accept | 1 commit |
| (f-3) **D-116 ms/초 통일** | real token expire check 위해 unit 통일 + manager.rs 갱신 | 1 commit |
| (f-4) **D-117 response_type 제거** | mock test 호환성 유지 | 1 commit (optional) |
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 20. 세션 — D-115 base_resp envelope 처리 (옵션 f-2)

**yklee 결정**: 옵션 f-2, D-114 의 deferred 1순위. Real `MiniMax` API 의 응답 envelope `base_resp.status_code` 분기 추가.

### 20.1 why
- D-113 의 real `request_code` 응답은 `{"base_resp": {"status_code": 0, "status_msg": "success"}, ...}` envelope 형식.
- D-114 의 deferred note: real `poll_token` 도 동일 envelope 일 가능성 → `status_code != 0` 분기 없으면 `TokenPoll::Error("unknown status:")` 로 떨어질 위험.
- mock server 응답도 real spec 과 일치하도록 갱신 필요.

### 20.2 결정 사항
- **helper 추가**: `base_resp_status_code(value: &serde_json::Value) -> Option<i64>` — `{base_resp: {status_code: N}}` → `Some(N)`, 부재 시 `None`.
- **`request_code` 분기**: HTTP 200 + `base_resp.status_code != 0` → `DeviceError::Provider(format!("oauth/code base_resp status_code={code} status_msg={msg}"))`. legacy `status: "error"` 분기는 유지.
- **`poll_token` 분기**: HTTP 200 + `base_resp.status_code != 0` → `TokenPoll::Error(format!("oauth/token base_resp status_code={code} status_msg={msg}"))`. legacy `status: "error"` 분기는 유지. **양쪽 envelope 모두 accept**.
- **mock server 갱신**: manager.rs 의 4개 mock 응답 (2 test × code + token) 에 `base_resp.status_code=0` envelope 추가. legacy `status:"success"` 동시 emit (회귀 0).
- **clippy 대응**: let-chain 으로 `collapsible_if` 회피.

### 20.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/auth/src/device_flow.rs` | helper `base_resp_status_code` + `request_code` / `poll_token` 의 `base_resp` 분기 (let-chain) + **6 신규 test** (3 unit + 3 e2e) + `tokio::io::{AsyncReadExt, AsyncWriteExt}` import to test module |
| `myharness/crates/auth/src/manager.rs` | mock server 응답 4개 (2 test × code + token) 에 `base_resp.status_code=0` envelope 추가 |

### 20.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** (let-chain 으로 collapsible_if 회피) |
| `cargo test -p myharness-auth --lib` | ✅ **48/48 pass + 1 ignored** (baseline 42 → +6 신규 D-115 test, 회귀 0) |
| `cargo test -p myharness-auth minimax_real_device_request_code -- --ignored --nocapture` | ✅ **PASS** (real network, real 응답의 `base_resp.status_code=0` 분기 통과) |
| `cargo test --workspace --lib` | ✅ **500 pass + 0 fail + 4 ignored** (D-114 baseline 494 → +6, 회귀 0) |

### 20.5 효과
- real `MiniMax` API 의 `base_resp.status_code != 0` 실패 (`40001 invalid_client`, `40004 invalid_grant` 등) 즉시 `TokenPoll::Error` 로 surface.
- token 단계 (`poll_token`) 의 real polling 동작 보장.
- D-115 spec = mock spec = 1:1 (real envelope 동시 emit 으로 회귀 0).
- D-113 real test 그대로 PASS.

### 20.6 한계 / 후속
- **D-116 (deferred)**: real API 의 `expired_in` / `interval` 이 epoch ms 인데 우리 `DeviceAuthorization::expired_in: u64` / `interval: u64` + `manager.rs` 의 `is_expired` check 는 초 가정. real token expire check 1000배 영향.
- **D-117 (optional, deferred)**: `response_type=code` 파라미터 제거. real spec 에 없음, mock test 호환성 위해 유지.

### 20.7 누적 결정
62 → **63** (D-115 추가).

### 20.8 main / push
- main = D-115 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 20.9 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (f-3) **D-116 ms/초 통일** | `DeviceAuthorization::expired_in` / `interval` + manager.rs 의 expire check 모두 ms 로 통일. 6 file (`device_flow.rs` + `manager.rs` + `store.rs` + mock test 등). 1 commit. | 1 commit |
| (f-4) **D-117 response_type 제거** | `request_code` 의 form body 에서 `response_type=code` 제거. mock test 회귀 0 확인. 1 commit (optional). | 1 commit |
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 21. 세션 — D-116 DeviceAuthorization 단위 contract 명시 (옵션 f-3)

**yklee 결정**: 옵션 f-3, D-114 의 deferred 2순위. Real `MiniMax` API 의 `expired_in` / `interval` 단위 (ms) 를 doc-comment + mock spec 으로 일원화.

### 21.1 why
- Real `MiniMax` API 응답: `interval=3000` (ms = 3초), `expired_in=1782881072225` (ms unix timestamp).
- 우리 `DeviceAuthorization::expired_in` / `interval` / `TokenPoll::Success::expired_in` / `DeviceToken::expired_in` 의 doc-comment 가 unit 명시 없음 → contributor 혼란 가능.
- Mock test 의 `interval=1` (legacy 초) → real spec mismatch → D-113 의 `interval > 0` assert 만은 진짜 단위 검증 부족.
- `expired_in_to_chrono` (manager.rs W14.7) 가 이미 ms/μs/s auto-detect → production 안전망 ✓.

### 21.2 결정 사항 (option C: doc-comment + mock 만)
- **doc-comment 4 곳 단위 명시**: `DeviceAuthorization::expired_in` (ms) / `::interval` (ms) / `TokenPoll::Success::expired_in` (ms) / `DeviceToken::expired_in` (ms).
- **`default_interval()`** 변경: `2` (legacy 초) → `2_000` (2초 = 2000ms).
- **`poll_until_success` signature**: `interval: u64` → `interval_ms: u64` + 내부에서 `(interval_ms / 1000).clamp(1, 10)` seconds 변환. mock (1초) / real (3초) 모두 sleep 정상.
- **Mock test spec 갱신**: `interval=1` → `1000` / `expired_in=now+60` → `now+60_000` (2 곳 × 2 test).
- **Mock test assertion 2곳**: `req.authorization.interval == 1` → `1_000`.
- **2 신규 invariant test**: `d116_interval_is_milliseconds_unit` + `d116_expired_in_is_milliseconds_unix_timestamp`.
- **유지**: `expired_in_to_chrono` (W14.7) 의 ms/μs/s auto-detect — production 안전망.

### 21.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/auth/src/device_flow.rs` | doc-comment 4 곳 + `default_interval` 2→2_000 + `poll_until_success(interval_ms, ...)` + `(interval_ms / 1000).clamp(1, 10)` 변환 + 2 신규 invariant test |
| `myharness/crates/auth/src/manager.rs` | mock server 응답 4 곳 (interval/expired_in) ms 갱신 + assertion 2곳 (`1`→`1_000`) |

### 21.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-auth --lib` | ✅ **50/50 pass + 1 ignored** (D-115 baseline 48 → +2, 회귀 0) |
| `cargo test -p myharness-auth minimax_real_device_request_code -- --ignored --nocapture` | ✅ **PASS** (real `interval=3000` ms invariant 그대로 통과) |
| `cargo test --workspace --lib` | ✅ **502 pass + 0 fail + 4 ignored** |

### 21.5 효과
- mock spec = real spec (ms) — `poll_until_success` 의 `interval_ms / 1000` 변환으로 통일.
- D-116 invariant test 가 추후 spec 변경 (e.g. seconds 전환) 시 즉시 fail.
- doc-comment 4 곳 단위 명시 → 새 contributor 의 혼란 방지.
- `expired_in_to_chrono` 의 auto-detect 안전망 유지로 backward compat 보장.

### 21.6 한계 / 후속
- **D-117 (optional, deferred)**: `response_type=code` 파라미터 제거. real spec 에 없음, mock test 호환성 위해 유지. v1.5+.
- `DeviceAuthorization::default_interval` 의 legacy mock 호환 (2초=2000ms) — mock test 가 `interval=1000` 으로 갱신됐지만 default path 는 아직 legacy 2초. **mock test 의 응답이 항상 `interval=1000` 으로 명시 emit** 하므로 default branch 는 unused. v1.5+ 에서 `default_interval` 을 `1_000` 으로 통일 검토.

### 21.7 누적 결정
63 → **64** (D-116 추가).

### 21.8 main / push
- main = D-116 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 21.9 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (f-4) **D-117 response_type 제거** | `request_code` form body 에서 `response_type=code` 제거 + mock test 회귀 0 확인 | 1 commit (optional) |
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 22. 세션 — D-117 request_code response_type=code 제거 (옵션 f-4)

**yklee 결정**: 옵션 f-4, D-114 의 deferred 3순위 (optional). Real `MiniMax` Device Authorization Grant spec 은 `response_type=code` 미포함.

### 22.1 why
- 우리 v1 의 `device_flow.rs::request_code` 가 `flow.rs:121` (Authorization Code + redirect 표준) 의 `response_type=code` 와 혼동해서 emit 했었음.
- real `MiniMax` Device flow spec 은 `response_type` 미포함 (RFC 8628 표준에도 없음). real API 가 무시하므로 production 영향 없지만, spec 정합성 위해 제거.
- mock test 의 JSON body decode 가 `response_type` 무관 → 회귀 0.

### 22.2 결정 사항
- `request_code` form body 에서 `("response_type", "code".to_string())` 한 줄 제거.
- `flow.rs:121` (Authorization Code + redirect 표준) 은 손대지 않음. 다른 spec.
- manager.rs 의 `login_non_interactive_returns_url_only` assertion 은 `MinimaxOAuth` (Authorization Code path) 의 URL 검증 → 무관.
- **1 신규 test** (`d117_request_code_form_body_omits_response_type`): form body 가 `response_type` 미포함 + PKCE/state/client_id/scope sanity.

### 22.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/auth/src/device_flow.rs` | `request_code` form body 에서 `response_type` 제거 (1 line) + D-117 doc-comment + **1 신규 test** (form body capture + 6 assert) |

### 22.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** (dead field fix 포함) |
| `cargo test -p myharness-auth --lib` | ✅ **51/51 pass + 1 ignored** (D-116 baseline 50 → +1, 회귀 0) |
| `cargo test -p myharness-auth minimax_real_device_request_code -- --ignored --nocapture` | ✅ **PASS** (real API 의 `response_type` 무시 동작 그대로) |
| `cargo test --workspace --lib` | ✅ **503 pass + 0 fail + 4 ignored** |

### 22.5 효과
- real `MiniMax` Device flow spec 1:1 정합.
- mock spec = real spec (둘 다 `response_type` 미포함).
- W14 mock test 7개 회귀 0.
- D-114 의 deferred list (D-115/116/117) **모두 완료** — real MiniMax OAuth flow 의 production-ready 검증 cycle 종료.

### 22.6 누적 결정
64 → **65** (D-117 추가).

### 22.7 main / push
- main = D-117 commit (코드 + 메모리 단일 push 2 commit, hash push 후 확정).

### 22.8 next 1순위 (D-114 cycle 종료, 다른 backlog 로)
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (g) TUI shell 검증 | binary 실행 + LoopRunner | smoke |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) D-109+ Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 23. 세션 — D-118 TUI shell render snapshot-style test 4종 추가 (옵션 g)

**yklee 결정**: 옵션 g, D-114~117 cycle 종료 후 TUI shell 검증. 기존 tui crate 9개 test (입력/렌더/keymap/엔터/CtrlC) 가 모두 PASS지만, 시각적/구조적 회귀 (role prefix 깨짐, header 사라짐, status 카운트 오류, draw 경로와 buffer helper 경로 비대칭) 를 잡는 가드가 약함.

### 23.1 why
- 기존 9개 test 는 happy-path + 일부 동작만 검증. multi-role rendering / header line / status count / 양 경로 동등성 등 *snapshot-style* 회귀 가드 부재.
- ratatui `TestBackend` 와 직접 `Buffer` 경로를 모두 쓰는 패턴이 `app.rs` 안에 공존 → 둘 중 한쪽만 깨지는 regression 을 잡으려면 양쪽 결과 비교가 필요.
- TUI 는 최종 사용자 가시 surface — 회귀 시 즉시 체감됨. 정량 가드 가치 큼.

### 23.2 결정 사항
`myharness/crates/tui/src/app.rs::tests` 모듈에 4개 test 추가:
1. **`d118_render_renders_all_role_prefixes`** — 5 role (`System`/`User`/`Assistant`/`Tool`/`Error`) 모두 push 후 80x24 buffer 에 `[sys]`/`[you]`/`[bot]`/`[tool]`/`[err]` prefix + 본문 등장 확인 (10 assert)
2. **`d118_render_header_includes_title_and_mode`** — 상단 header line 에 `title` 텍스트 + `[mode]` 태그 텍스트 존재 확인
3. **`d118_render_status_reflects_message_count`** — welcome 1 + push 4 = 5 → status line `5 msg` 정확성, `4 msg` 부재 확인
4. **`d118_draw_and_render_to_buffer_agree_on_text`** — `Terminal::draw` 경로와 `render_to_buffer` 직접 경로의 동일 input → 동일 text token (`agree`, `[loop]`, `[you]`, `hello`, `[bot]`, `world`) 등장 확인

helper: `fn buffer_text(buf: &Buffer) -> String` — cell symbol 평문화.

### 23.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/tui/src/app.rs` | test 모듈에 helper 1개 + 4개 test 추가 (~95 lines, production code 무변경) |

### 23.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build -p myharness-tui` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-tui d118` | ✅ **4/4 pass** (4 신규 test) |
| `cargo test -p myharness-tui --lib` | ✅ **79/79 pass** (baseline 75 → +4, 회귀 0) |
| `cargo test --workspace --lib --no-fail-fast` | ✅ **507 pass + 0 fail + 4 ignored** (D-117 503 → +4) |

### 23.5 효과
- TUI render 회귀 가드 4개 추가 — role prefix / header / status count / draw-buffer 양 경로 동등성.
- 기존 동작 / API / production code 변경 0 — 순수 test 만 추가.
- 회귀 0 (tui crate 75 → 79, workspace 503 → 507).

### 23.6 누적 결정
65 → **66** (D-118 추가).

### 23.7 main / push
- main = D-118 commit (코드 + 메모리 단일 push 1 commit, hash push 후 확정).

### 23.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (h) 추가 안정화 | cargo hygiene | 1 commit |
| (i) Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |
| (g-추가) TUI 검증 보강 | SubAgent dispatch 통합 test 또는 AppKey 엣지케이스 | 1 commit |

## 24. 세션 — D-119 workspace-level lints baseline 추가 (옵션 h-1)

**yklee 결정**: 옵션 h-1, cargo hygiene. 옵션 g (D-118) 종료 후 추가 안정화 사이클 시작. 현재 cargo hygiene baseline 점검 결과:
- workspace `[lints]` 섹션 부재 (8 crate 모두 lints 0)
- `cargo fmt --check` 시 **511 line diff** (대규모 formatting drift)
- 6 baseline lint 후보 모두 `warn` 으로 추가 시 새 warning 0개로 통과 확인

### 24.1 why
- workspace-level lints 가 SSOT 1 곳 (Cargo.toml) 에 정의 → 8 crate 가 자동 상속. 일관성 ↑.
- D-113~118 사이클에서 D-114~D-117의 deferred list 가 모두 해소 → hygiene 으로 attention 전환.
- 6개 baseline lint (3 rust + 3 clippy) 는 모두 안전한 hygiene 가드: 기존 코드 위반 0 → warning 0개. clippy -D warnings 통과.
- h-2 (cargo fmt 511 line 일괄) 는 사이즈가 너무 큼 → 별도 사이클 (D-120+) 로 분리.

### 24.2 결정 사항
`myharness/Cargo.toml` 의 `[profile.release]` 다음에 workspace-level lints 추가 (Cargo 1.74+ `[workspace.lints.*]`):

**`[workspace.lints.rust]`** (모두 `warn`):
- `unsafe_code` — 모든 unsafe 사용 시 경고 (현재 26+ but 모두 add_local.rs test code 의 env::set_var/remove_var)
- `missing_debug_implementations` — pub struct 의 Debug 미구현 시 경고
- `rust_2018_idioms` — `..Default::default()` 등 deprecated 패턴 경고

**`[workspace.lints.clippy]`** (모두 `warn`):
- `module_name_repetitions` — `foo::Foo` 같은 자명한 이름
- `needless_pass_by_value` — `&T` 로 충분한데 `T` 로 받는 경우
- `redundant_closure_for_method_calls` — `|x| x.method()` → `Method::method`

### 24.3 변경
| file | 변경 |
|---|---|
| `myharness/Cargo.toml` | `[workspace.lints.rust]` + `[workspace.lints.clippy]` baseline 6개 lint 추가 (production code 0 변경) |

### 24.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets` | ✅ 0 warning (새 lint 6개로 인한 warning 0개) |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning |
| `cargo test --workspace --lib --no-fail-fast` | ✅ **507 pass + 0 fail + 4 ignored** (D-118 507 → 동, 회귀 0) |

### 24.5 효과
- 1 곳에서 lint 정책 SSOT — 8 crate 자동 상속. 새 crate 추가 시 자동 적용.
- 강한 deny/forbid 정책 (unsafe_code, missing_docs 등) 은 opt-in (향후 의도적 활성화 가능).
- 0 warning 으로 hygiene baseline 확립 — 향후 새 lint 추가도 baseline 에 영향 없음.
- h-2 (cargo fmt) 와 분리 — 작은 사이즈 commit 으로 review 용이.

### 24.6 누적 결정
66 → **67** (D-119 추가).

### 24.7 main / push
- main = D-119 commit (코드 + 메모리 단일 push 1 commit, hash push 후 확정).

### 24.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (h-2) cargo fmt 일괄 적용 | 511 line diff 자동 적용, large commit | 별도 사이클 |
| (i) Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |
| (g-추가) TUI 검증 보강 | SubAgent dispatch 통합 test 또는 AppKey 엣지케이스 | 1 commit |

## 25. 세션 — D-120 cargo fmt 일괄 적용 (옵션 h-2)

**yklee 결정**: 옵션 h-2, h scope 종료. D-119 (h-1, workspace lints) 종료 후 별도 사이클로 분리했던 cargo fmt 511 line drift 해소.

### 25.1 why
- D-119 (h-1) 종료 시점 baseline 검사에서 `cargo fmt --check` 가 **511 line diff** 보고됨.
- 511 line drift 는 rustfmt 의 stable formatting 과 workspace 코드 사이 누적된 차이 — production 의미 0, 단순 whitespace.
- large commit 이지만 의미 변경 0 이라 안전. h-2 = 1 commit 으로 흡수.
- h-1 + h-2 = h scope 종료. 다음 사이클은 (B)/(e)/(i)/(j)/(g-추가).

### 25.2 결정 사항
- `cargo fmt` 일괄 적용 (rustfmt default config, 8 crate 모두 영향).
- **67 files / +1980 / -948** (rustfmt whitespace reformat).
- 의미 변경 0: struct literal multi-line 강제, function arg multi-line 강제, closure 위치, return type 위치, 등 표준화.
- `cargo fmt --check` 0 diff — drift 완전 해소.

### 25.3 변경
| file | 변경 |
|---|---|
| 67 files (8 crate 전체) | rustfmt whitespace reformat (+1980 / -948) |

### 25.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 warning |
| `cargo test --workspace --lib --no-fail-fast` | ✅ **507 pass + 0 fail + 4 ignored** (D-119 507 → 동, 회귀 0) |
| `cargo fmt --check` | ✅ 0 diff (drift 완전 해소) |

### 25.5 효과
- rustfmt 와 workspace 코드 1:1 정합. 향후 `cargo fmt` drift 0.
- 의미 변경 0 — 모든 test / build / clippy 통과. 1 commit 으로 흡수.
- h scope 종료: h-1 (D-119) + h-2 (D-120). 다음 사이클로 (B) Anthropic / (e) TASK-002 / (i) Lark / (j) block-aware / (g-추가) TUI SubAgent dispatch.

### 25.6 누적 결정
67 → **68** (D-120 추가).

### 25.7 main / push
- main = D-120 commit (코드 67 files + 메모리 단일 push 1 commit, hash push 후 확정).

### 25.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (i) Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |
| (g-추가) TUI 검증 보강 | SubAgent dispatch 통합 test 또는 AppKey 엣지케이스 | 1 commit |

## 26. 세션 — D-121 TUI SubAgent dispatch 통합 test 4종 추가 (옵션 g-추가)

**yklee 결정**: 옵션 g-추가, TUI 검증 보강. D-120 (h-2 fmt) 종료 후 D-118 (옵션 g) 의 follow-up. 기존 tui test 79개 모두 PASS지만, `Orchestrator::dispatch` (prefix 6개 / keyword 3 domain / default) 와 `SubAgentRegistry` (4종 등록 + domain 분류) 의 *통합 가드* 부재.

### 26.1 why
- D-118 (옵션 g) 은 `app::render_to_buffer` 의 *시각적* 가드. D-121 은 *dispatch logic* 가드 — 6 prefix × 정확한 kind, code/env/git_kw fallback, default 분기, registry 4종 + domain 분류 2/1/1.
- LLM 호출 없는 headless path — mock LLM 불필요. 정확성 검증에 최적.
- 4 SubAgent 가 `def().kind.as_str()` 으로 `code-reviewer`/`code-implementer`/`env-diagnose`/`git-operator` 4종 이름을 보장하는지도 회귀 가드.
- (2) 옵션 (AppKey 엣지케이스) 는 미수행 — 기존 5개 test + Release 이벤트 자연 reject 으로 충분.

### 26.2 결정 사항
`myharness/crates/tui/src/orchestrator.rs::tests` 모듈에 4개 test 추가:
1. **`d121_dispatch_prefix_each_subagent`** — 6 prefix (`code review`/`code implement`/`code refactor`/`env diagnose`/`git `/`git-operator`) 각각이 `SubAgentKind` + `DispatchKind::Direct` + `extracted_input` 검증. prefix 만 있고 extracted 비면 user_input 그대로.
2. **`d121_dispatch_domain_keyword_fallback`** — prefix 매칭 실패 후 `code_kw` → `CodeReviewer`, `env_kw` → `EnvDiagnose`, `git_kw` → `GitOperator` (모두 `DispatchKind::DomainKeyword`).
3. **`d121_dispatch_default_fallback`** — `hello world` / `!!! ? ?` → `Default` 분기 (`CodeReviewer` + `DispatchKind::Default`).
4. **`d121_subagent_registry_4_unique`** — `SubAgentRegistry::all()` 4개 + `for_kind` 4종 모두 `Some` + `by_domain(Code)`=2, `by_domain(Environment)`=1, `by_domain(Utility)`=1 + `by_name` 동작.

### 26.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/tui/src/orchestrator.rs` | test 모듈에 4개 dispatch 통합 test 추가 (~110 lines, production code 무변경) |

### 26.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-tui d121` | ✅ **4/4 pass** (4 신규 test) |
| `cargo test -p myharness-tui --lib` | ✅ **83/83 pass** (baseline 79 → +4, 회귀 0) |
| `cargo test --workspace --lib --no-fail-fast` | ✅ **511 pass + 0 fail + 4 ignored** (D-120 507 → +4) |

### 26.5 효과
- SubAgent dispatch 정확성 4개 가드 — prefix table, keyword fallback, default, registry 일관성.
- 기존 dispatch / SubAgent / domain 매핑 회귀 0 — 4개 test 모두 production code 와 1:1 매칭.
- D-118 (시각적) + D-121 (dispatch) = TUI 검증 보강 양 축 완성.

### 26.6 누적 결정
68 → **69** (D-121 추가).

### 26.7 main / push
- main = D-121 commit (코드 + 메모리 단일 push 1 commit, hash push 후 확정).

### 26.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (B) Anthropic wire format | content tool_use block parse, test-only mock server | 1-2 commit |
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (i) Lark multi-section parser | cross-file patch | 큰 사이클 |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |

## 27. 세션 — D-122 Anthropic wire format tool_use block parse + mock server test (옵션 B)

**yklee 결정**: 옵션 B. 옵션 i (Lark) 는 source code 에 Lark 부재 (handoff 메모리에만 "Lark multi-section parser" 언급, 의미 정의 불가) → yklee 가 보류 결정. 다음 후보로 (B) Anthropic wire format 진행.

### 27.1 why
- `client_anthropic.rs` 의 `complete()` 가 `tool_calls: Vec::new()` 로 **항상 비어있음**. `AssistantContent::ToolCall` variant 무시, `extract_text` 만 호출.
- D-108 follow-up (OpenAI-compat wire format) 의 패턴을 따라 hand-rolled Anthropic `/v1/messages` POST + response `content[].tool_use` block parse.
- rig-core 0.38.1 의 `anthropic::completion` 가 이미 `AssistantContent::ToolCall` 변환 지원 — `extract_tool_calls` helper 로 우리 `ToolCall` shape normalize.
- handoff 메모리의 "test-only mock server" 요구 → TcpListener 기반 mock 으로 wire format 검증 (real network 불필요).

### 27.2 결정 사항
`myharness/crates/llm/src/client_anthropic.rs`:
1. `AnthropicProvider` 에 `api_key: String`, `base_url: String` field + `with_base_url()` builder 추가.
2. `complete()` 진입 시 `!req.tools.is_empty()` 이면 `complete_wire_format()` 으로 분기.
3. `complete_wire_format`: reqwest POST `{base_url}/v1/messages` (header: x-api-key, anthropic-version: 2023-06-01, Content-Type: application/json). payload: `{model, messages, system?, max_tokens?, temperature?, tools?}` (Anthropic native shape, `tools[].{name, description, input_schema}`).
4. `parse_anthropic_response`: `serde_json` content[] 를 `text`/`tool_use`/`other` variant 로 deserialize. `tool_use` → `ToolCall {id, name, arguments}`. usage / stop_reason / model 보존.
5. `extract_tool_calls()` helper: rig-core native path 의 `AssistantContent::ToolCall` 도 동일 shape 로 변환.
6. **1 신규 mock server test** `d122_wire_format_parses_tool_use_block`: TcpListener 가 text + tool_use 동시 emit → 우리 parser 가 content 정확 분리 + `tool_calls[0]` 의 `id="toolu_test_01"`, `name="Read"`, `arguments={path, limit}` 검증. request sanity (tools + tool name "Read" wire) 도 검증.

### 27.3 변경
| file | 변경 |
|---|---|
| `myharness/crates/llm/src/client_anthropic.rs` | struct + field + builder + wire format path + parser + helper + 1 mock test (+~280 lines) |

### 27.4 3-way verify
| check | 결과 |
|---|---|
| `cargo build --workspace` | ✅ clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ **0 warning** |
| `cargo test -p myharness-llm d122` | ✅ **1/1 pass** |
| `cargo test -p myharness-llm --lib` | ✅ **131/131 pass + 3 ignored** (baseline 128 → +3, 회귀 0) |
| `cargo test --workspace --lib --no-fail-fast` | ✅ **512 pass + 0 fail + 4 ignored** (D-121 511 → +1) |

### 27.5 효과
- Anthropic native tool calling 지원. text + tool_use 동시 응답 정확히 분리.
- OpenAI-compat (D-108 follow-up) 와 동일 패턴. Anthropic 만 wire format 다름 (x-api-key, anthropic-version 헤더, top-level system, tools[].input_schema).
- mock server test → real API 호출 없이 wire format 검증. CI 안전.
- rig-core native path + hand-rolled path 둘 다 `ToolCall` 동일 shape → caller 가 provider 무관.

### 27.6 누적 결정
69 → **70** (D-122 추가).

### 27.7 main / push
- main = D-122 commit (코드 + 메모리 단일 push 1 commit, hash push 후 확정).

### 27.8 next 1순위
| 후보 | scope | effort |
|---|---|---|
| (e) TASK-002 도메인 명령 | yklee 인프라 정보 의존 | 1 commit |
| (j) D-109+ block-aware insert/replace | `replace block N` + `insert before/after` | 1-2 commit |
| (B-추가) Anthropic streaming / tool_result 매핑 / system 검증 | 추가 mock test 2-3개 | 1 commit |
| (i) Lark multi-section parser | 의미 정의 후 재개 | TBD |
