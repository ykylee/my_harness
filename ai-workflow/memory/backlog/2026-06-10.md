# 일별 백로그: 2026-06-10

- 목적: D-62 follow-up — TC-W17-002 test fail 복구 + W17 누락분 정정 (D-62) 의 W19-1 결과 기록 및 다음 행동 정의
- 범위: W19-1 결정 (AuthStore 주입 패턴), test fail 원인 분석, 신규 4 L1 test, side effect, 다음 작업 단계
- 대상 독자: yklee, opencode orchestrator, doc-worker
- 상태: active
- 최종 수정일: 2026-06-10
- 관련 문서: [세션 인계](../session_handoff.md), [작업 백로그 인덱스](../work_backlog.md), [state.json](../state.json), [CONCEPT.md](../../docs/CONCEPT.md)

## 1. 결정 (D-62 W19-1)

- **TC-W17-002 test fail 복구** — AuthStore 트레이트 객체 주입 패턴 도입. 기존 fn 은 thin wrapper. cli caller 변경 없음 (back-compat). 110 llm tests / 0 fail. cargo build/clippy OK
- **W17 누락분 = 0건 정정** (D-62, 2026-06-10) — W18 정합성 cross-check 가 잘못된 결론. W17 original 4 commit (766d1b6/a46f480/f8082bc/d09a6a1) 모두 main 머지 확인. cleanup commit 6e925a1 가 W18 cherry-pick 중복 fn/test 만 제거하고 W17 본진 보존

## 2. TC-W17-002 fail 원인 분석

### 증상
- `cargo test --workspace --lib` 실행 시 `add_local::tests::tc_w17_002_non_interactive_with_token` 1 fail
- panic 위치: `crates/llm/src/add_local.rs:620` (현재 line ~664)
- 에러: `called Result::unwrap() on an Err value: BackendUnavailable("set MYHARNESS_LOCAL_LLM_KEY=... env, or install libsecret-1-dev + gnome-keyring for persistent storage")`

### 원인
- test 가 caller 측에서 `KeyringAuthStore::probe()` 1회 (store A) 만들고, `register_local_provider_non_interactive` 내부에서 `KeyringAuthStore::probe()` 다시 호출 (store B)
- `KeyringAuthStore` 의 in-memory cache 가 `Mutex<HashMap<ProviderId, String>>` (instance 별) → store A 와 store B 가 **별개 cache**
- `register_local_provider` 내부 set 은 store B cache 에 들어가서 caller 측 store A cache 에는 없음
- test 가 store A 로 `get` 시도 → cache miss + backend=None → `Err(BackendUnavailable)` → `.unwrap()` panic

### 교훈 (D-62 lesson 확장)
- in-memory cache 가 instance-scoped 이면 caller 와 callee 가 같은 instance 공유 안 함
- 해결: caller 가 store 1개 만들어 함수에 명시 전달 → cache lifecycle 단일화
- back-compat: 기존 fn 시그니처 유지 + 새 `with_store` variant 추가

## 3. W19-1 변경 사항

### 3.1 신규 public API (`myharness/crates/llm/src/add_local.rs`)

```rust
/// W19-1 (D-62 follow-up) — caller 가 store lifecycle 명시적 제어
pub async fn register_local_provider_with_store(
    base_url: &str,
    token: Option<&str>,
    selected_model: &ModelInfo,
    available_models: &[ModelInfo],
    store: &dyn AuthStore,
) -> Result<RegisterReport, RegisterError>;

pub async fn register_local_provider_non_interactive_with_store(
    base_url: &str,
    token: Option<&str>,
    model_id: &str,
    store: &dyn AuthStore,
) -> Result<RegisterReport, RegisterError>;
```

### 3.2 기존 fn 변경
- `register_local_provider(base_url, token, selected, available)` 시그니처 **유지** (back-compat)
- 본체: `KeyringAuthStore::probe()` 1회 + `register_local_provider_with_store` 호출하는 thin wrapper
- `register_local_provider_non_interactive(base_url, token, model_id)` 동일

### 3.3 lib.rs export 추가
- `register_local_provider_with_store` / `register_local_provider_non_interactive_with_store` re-export

### 3.4 Test 변경 (4 L1)
- **TC-W17-002 수정**: `register_local_provider_non_interactive_with_store` 호출, 같은 store 로 `get` → cache hit
- **TC-W19-001 신규**: `register_local_provider_with_store` + 같은 store → cache hit 보장
- **TC-W19-002 신규**: store 주입 + `token = None` → `token_saved = false` + cache 무변경
- **TC-W19-003 신규**: thin wrapper 의 `register_local_provider` 가 caller 와 별개 store 생성 (회귀 방지)

## 4. 검증 결과

### cargo build --workspace
- OK (1.65s)
- 신규 warning 0건 (W18 기존 warning 만 유지)

### cargo test --workspace --lib
- **110 passed, 0 failed, 2 ignored** (myharness-llm)
- 전체: 42 + 40 + 56 + 32 + 110 + 51 + 51 = 382 (lib) + 51 (cli) = 433
- 기존 baseline 411 + W19-1 신규 3 L1 test = 414 (cli 일부 추가로 433)

### cargo clippy --workspace --all-targets
- 7 myharness-llm warnings (모두 W18 이전 기존, W19-1 신규 0건)
- 56 total warnings (W18 이전 + 기존 legacy)

## 5. Git history

- 1 commit: `feat(llm): W19-1 TC-W17-002 test fail 복구 (AuthStore 주입 패턴) + register_local_provider_with_store 추가 (D-62 follow-up)`
- 변경: `add_local.rs` +178 / -14, `lib.rs` +1
- Gitea origin + GitHub upstream dual-push

## 6. Side effect

- cli caller (main.rs) 3개 변경 없음 — 기존 fn (back-compat wrapper) 가 caller 의 store lifecycle 무관하게 동작
- 향후 cli 가 store lifecycle 명시 제어가 필요하면 `with_store` variant 로 마이그레이션 가능 (back-compat)

## 7. 다음 행동

1. (선택) **cli caller 도 `with_store` variant 로 마이그레이션** — caller 가 store lifecycle 명시, 추후 multi-store 환경 또는 auth 상태 진단 시 유리
2. TASK-005-2 W19+ 결정 — yklee 결정
   - F-1 monotonic_ts (sub-second backup 충돌 방지)
   - F-2 git-style versioning (TS+content hash)
   - F-3 Ollama native `/api/tags` 지원
   - Plugin 4-계층 (큰 사이클)
   - CCR + Kompress-base ONNX
3. MiniMax Device OAuth real flow 검증 (W15.b 자동 refresh real test)
4. ANTHROPIC_API_KEY 주입 시 LLM E2E 테스트 (real-anthropic ignored)
5. TASK-002 (도메인별 명령) — yklee 인프라 정보 수령 후
6. Gitea PAT 회전 (이전 세션 노출)

## 8. 발견된 이슈 갱신

- **AuthStore 의 instance-scoped in-memory cache** (D-62 W19-1): 동일 type 의 여러 instance 가 같은 backend 를 공유하지 않음 (의도된 설계이나 caller-주도 cache lifecycle 필요). v1.5+ 에서 `KeyringAuthStore` 가 `Arc<KeyringAuthStore>` (process-wide shared) 또는 static LazyLock 가 되면 caller-주도 lifecycle 불요. 현 v1.5+ 에서는 caller 가 store 1개 만들어 명시 전달 패턴 권장
- **libsecret 부재 환경 BackendUnavailable** (KeyringAuthStore::get): in-memory cache hit → Ok, miss → Err (의도된 v1 동작). 영구 저장 미지원 환경에서는 set → get 사이클만 동작, clear 후 재시도 시 Err. v1.5+ 에서 in-memory cache 만으로 동작하도록 backend=None 도 Ok(None) 으로 softening 검토 (단, 사용자 혼동 방지 위해 warn log 추가)

## 9. D-63 (2026-06-10) — W20 F-3 Ollama native /api/tags cascade

### 결정 배경
- 3개 후보 (F-1 monotonic_ts, F-2 git-style versioning, F-3 Ollama native) 검토 후 우선순위 결정:
  1. **F-3 우선** (Ollama 사용자 영향 큼, 가장 독립적, 0.5d)
  2. F-1+F-2 통합 (다음 사이클)
  3. Plugin 4-계층 (큰 사이클)
- 3 background agent (explore) 가 parallel 분석 완료 (총 ~7분)

### Split-brain 발견
- `scan_local.rs:27` — **이미 Ollama native `/api/tags` 사용 중** (discover 용)
- `add_local.rs:92` (이전) — OpenAI 호환 `/v1/models` 만 사용 (probe 용)
- **결과**: Ollama default 모드 (native only) 사용자가 `auth add-local` 시 404

### 구현 (D-63)

#### `probe_local_models` 2-stage cascade
```rust
pub async fn probe_local_models(base_url: &str, token: Option<&str>) -> Result<Vec<ModelInfo>, RegisterError> {
    let base = base_url.trim_end_matches('/');
    // Stage 1: Ollama native `/api/tags`
    match probe_ollama_tags(base, token).await {
        Ok(Some(models)) if !models.is_empty() => return Ok(models),
        Ok(_) | Err(_) => { /* fallback */ }
    }
    // Stage 2: OpenAI 호환 `/v1/models` (back-compat W16)
    probe_openai_models(base, token).await
}
```

#### Helper 3개 분리
- `probe_ollama_tags(base, token) -> Result<Option<Vec<ModelInfo>>, _>` — 404/405 는 `Ok(None)` 으로 cascade
- `probe_openai_models(base, token) -> Result<Vec<ModelInfo>, _>` — `ends_with("/v1")` 분기로 back-compat
- `fetch_json_body(url, token, timeout) -> Result<Value, _>` — 공통 HTTP

#### Parser
- `parse_ollama_tags(body) -> Vec<ModelInfo>` — `models[*].name` → id, `models[*].details.family` → owned_by
- `parse_openai_models(body) -> Vec<ModelInfo>` — `data[*].id` → id, `data[*].owned_by` → owned_by (W16 원래 로직)

### L2 Integration Test 3개
- **TC-W20-I01**: Ollama native `/api/tags` 200 → cascade stage 1 성공, 2 모델 + family 매핑
- **TC-W20-I02**: `/api/tags` 404 → cascade stage 2 fallback → OpenAI JSON 성공
- **TC-W20-I03**: 양쪽 다 200 → native 우선 (early return, OpenAI 응답 미사용)

### 회귀 검증
- W16 integration tests 7/7 PASS (`tc_w16_i01/02/03`, `tc_w17_i01/02`, `tc_w18_i01/02`)
- 기존 `tc_w18_i01` 의 `sleep(1100ms)` 그대로 동작 (sub-second race 회피)

### 검증 결과
- `cargo build --workspace`: OK
- `cargo test --workspace --lib`: 110 llm + 32/40/56/42/51/51 lib = 382 / 0 fail
- `cargo test -p myharness-llm --test w16_add_local`: 7 (W16+W17+W18) + 3 (W20) = 10 / 0 fail
- `cargo clippy --workspace --all-targets`: 신규 warning 0건 (W19-1/W18 기존 warning 만 유지)

### Git history
- 1 commit: `feat(llm): W20 F-3 Ollama native /api/tags cascade (D-63) + parse_ollama_tags helper`
- 변경: add_local.rs (refactor + cascade) + w16_add_local.rs (3 L2)
- Gitea origin + GitHub upstream dual-push

### 다음 행동
1. **W21 F-1+F-2 통합** — F-2 권장 (sha2 8-char) + sort bug fix + F-1 random suffix 또는 단순히 ts 정밀도 향상. backup filename = `<ts>.<sha256_8>`. R-5-A (sub-second race) + R-5-B (corruption) + sort bug 동시 해결
2. MiniMax Device OAuth real flow 검증
3. ANTHROPIC_API_KEY 주입 시 LLM E2E 테스트
4. TASK-002 (도메인별 명령) — yklee 인프라 정보 수령 시
5. Gitea PAT 회전

### Side effect
- `fetch_json_body` helper 추가로 향후 probe endpoint 확장 (vLLM native, LM Studio native 등) 시 동일 패턴 재사용
- `parse_ollama_tags` / `parse_openai_models` 가 `pub(crate)` — 외부 노출 안 함, 향후 더 정교한 ModelInfo 확장 (size, format 등) 시 추가

## 10. D-64 (2026-06-10) — W21 F-1+F-2 통합 (backup filename `<ts>.<sha256_8>` + sort bug fix)

### 결정 배경
- F-1 (monotonic_ts) + F-2 (git-style versioning) 단독 검토 시 각각 효과 제한
- F-1: sub-second 충돌 방지하나 corruption 미검증
- F-2: content hash 로 collision + corruption 동시 해결, sha2=0.10 이미 auth crate 에 있음
- **F-1+F-2 통합** = `<unix_ts>.<sha256_8>` 한 사이클로 R-5-A/B + sort bug 동시 해결

### 3가지 Risk (구현 결정 근거)

#### R-5-A: Sub-second collision
- 기존 `as_secs()` 만 사용 시 동일 second 내 rapid register → 동일 filename → 앞 backup 덮어쓰기
- TC-W18-001/002 가 `sleep(1100ms)` 로 workaround 했음 (이제 불요)
- **해결**: hash8 으로 동일 ts 내 다른 content → 다른 hash → 다른 filename

#### R-5-B: Content 식별 불가
- 기존 filename = `<ts>` only → user 가 어떤 backup 이 어떤 content 인지 모름
- **해결**: hash8 = SHA-256(content)[:8] → backup file 의 content snapshot fingerprint

#### Sort bug (W21 background 분석에서 추가 발견)
- `cleanup_old_backups` 가 `sort_by_key(|e| e.file_name())` (string sort) 사용
- `backup.999` (string 정렬 시 4번째) vs `backup.10000` (string 정렬 시 1번째)
- '9' > '1' 으로 retention **거꾸로 동작** — 더 오래된 것이 살아남고, 더 새로운 것이 삭제됨
- **해결**: `parse unix_ts` numeric sort

### 구현

#### hash8 util (신규)
- `myharness/crates/llm/src/hash8.rs`
- `content_hash_8(content: &[u8]) -> String` — SHA-256 → hex 8-char truncate
- 의존성: `sha2 = "0.10"` (auth crate 에 이미 존재, llm crate 에만 1줄 추가)
- 3 L1 test (identical/different/empty)

#### backup 함수 3개 리팩토링
- `backup_providers_toml`: content 읽기 → hash8 계산 → `with_backup_suffix(path, ts, &hash8)` 호출
- `with_backup_suffix`: signature `(path, ts: u64, hash8: &str) -> PathBuf`, format = `<base>.backup.<ts>.<hash8>`
- `cleanup_old_backups`: **sort fix** — `parse unix_ts` numeric sort (string sort 거꾸로 retention 수정)

### L1 test 4개
- **TC-W21-001**: backup filename 이 `<ts>.<hash8>` 형식 + hash8 = 8-char hex 검증
- **TC-W21-002**: cleanup_old_backups numeric sort 정확도 — 8개 backup (ts 100~107) + 1 trigger (ts >= 108) → max=5 → 100~103 삭제, 104~107 + new trigger 보존
- **TC-W21-003**: sub-second 연속 backup (sleep 없이) — 3개 모두 다른 filename + content 보존 (R-5-A 검증)
- **TC-W21-004**: 동일 content 백업 시 hash 동일 (deterministic) — content fingerprint 검증

### 회귀 검증
- W16 7/7 PASS (tc_w16_i01/02/03, tc_w17_i01/02, tc_w18_i01/02 sleep(1100ms) 그대로 동작)
- W19-1 4/4 PASS (TC-W17-002/19-001/002/003)
- W20 3/3 PASS (TC-W20-I01/02/03)
- 117 llm + 10 w16_add_local + 3 hash8 = 130 / 0 fail
- 424 workspace tests / 0 fail (전체)

### 검증 결과
- `cargo build --workspace`: OK
- `cargo test --workspace`: 424 / 0 fail / 2 ignored
- `cargo clippy --workspace --all-targets`: 신규 warning 0건 (W19-1/W18 기존 warning 만)

### Git history
- 2 commit: hash8 util (with sha2 dep) + backup/sort fix (with TC)
- 또는 1 commit (workdir 단일) — 선택
- Gitea origin + GitHub upstream dual-push

### 다음 행동
1. **TASK-005-2 v1.5 종료 선언** — yklee 결정. v1.5 MVP 4 cycle (W17, W18, W19-1, W20, W21) + D-62 (W17 누락분 정정) + D-64 (W21 sort bug fix) 완료
2. 후보 (v1.5 후속):
   - Plugin 4-계층 (큰 사이클) — auto memory / provider-auto-config skill / marketplace
   - CCR + Kompress-base ONNX (W9 v1 simple 의 한계 — 도메인 명사 stemming)
   - v1.5 종료 + v2.0 (TUI/IDE/Web hand-off, Routines, OAuth, MCP-based discover)
3. yklee 인프라 정보 수령 시 TASK-002 (도메인별 명령)
4. Gitea PAT 회전 (이전 세션 노출)
5. MiniMax Device OAuth real flow 검증
6. ANTHROPIC_API_KEY 주입 시 LLM E2E 테스트

### Side effect
- TC-W18-001/002 의 `sleep(1100ms)` 가 불요하지만 back-compat 으로 그대로 동작 (sleep 없이도 hash8 으로 collision 방지)
- `with_backup_suffix` signature 변경 — internal fn (pub(crate)) 이라 외부 caller 영향 없음
- sha2 = 0.10 dep 추가 (auth crate 와 동일 버전, workspace dep 아님 — 향후 workspace 공통화 가능)

## 12. D-66 (2026-06-10) — TASK-005-2 v2.0 ONNX Commit 1 abort

### 결정 배경
- v1.5 종료 (D-65) 직후 v2.0 ONNX 3-commit 첫 작업 진입
- library 분석: ort v2.0.0-rc.12 (2026-03) 가 production-ready, MSRV 1.88 (workspace 와 일치), `download-binaries` default 로 C++ toolchain 불필요, all-MiniLM-L6-v2 ONNX int8 quantized 23MB HF download + SHA256 verify
- **library 분석의 위험**: 실제 cargo build 검증 안 함

### 빌드 검증 결과 (abort 결정)

#### 시도 1: ort = "=2.0.0-rc.12" (가장 최신)
```
ort-sys 2.0.0-rc.12 build script 실패:
error[E0599]: no method named `tls_config` found for struct `ConfigBuilder<Scope>`
   --> .../ort-sys-2.0.0-rc.12/build/download/mod.rs:45:5
```
**원인**: ureq 3.1 API 변경으로 `TlsConfig::builder()` API 호환 깨짐

#### 시도 2: ort = "1.13" (v1.13.1)
```
error: failed to select a version for the requirement `ort = "1.13"`
  version 1.13.1 is yanked
```
**원인**: ort 1.x **전부 yanked** (1.13.0/1/2/3, 1.14.x, 1.15.x, 1.16.0/1/2/3) — crates.io 에서 다운로드 불가

#### 시도 3: ort = "=1.16.3" (마지막 1.x)
```
error: version 1.16.3 is yanked
```
**원인**: 1.x 시리즈 모두 yanked 상태

#### 시도 4: ort = "=2.0.0-rc.10" (Jun 2025)
```
error[E0433]: failed to resolve: use of unresolved module or unlinked crate `tokio_stream`
error[E0425]: cannot find type `Session` in crate `ort`
error[E0599]: no method named `bytes_stream` found for struct `Response`
error[E0433]: failed to resolve: could not find `Session` in `ort`
```
**원인**: rc.10 API 가 rc.12 와 호환 안 됨. `Session` 위치 변경, `bytes_stream` 별도 feature 필요, `tokio_stream` 별도 의존성

#### 시도 5: ort = "=2.0.0-rc.9" (Nov 2024, 가장 오래된 rc)
```
error[E0599]: no method named `unwrap_or_else` found for fn pointer
  `unsafe extern "system" fn(*mut OrtLoraAdapter)`
... (수십 개)
```
**원인**: fn pointer 에 `unwrap_or_else` method 미구현, 2.0.0-rc.9 의 FFI binding 문제

### abort 결정
- **모든 변경 revert**: ort dep 제거, sha2 workspace dep 추가 안 함, onnx_model.rs 삭제
- **working tree clean**: `git status` — modified 0, untracked (AGENTS.md, .omo/ 만)
- **424 workspace tests 그대로 pass** (변경 영향 0)

### Lesson (D-66)
- **ecosystem stability 는 SSOT** (CONCEPT.md §11.3)
- library 분석 시 **반드시** crates.io API + lib.rs + **실제 cargo build** 검증 필수
- library 보고가 outdated 일 수 있음 (ort 2.0.0-rc.12 가 "production-ready" 였지만 ureq 3.1 API 변경으로 빌드 깨짐)
- **1-2 RC 후 재시도** 또는 **tract (Pure Rust) 대안** 검토

### 차후 옵션 (v2.0)
- **(a) tract** (Pure Rust ONNX runtime): ort 보다 안정적, 모델 ecosystem 작음, binary size 더 작음. ONNX file 직접 parse. 가능성 있음
- **(b) ort 안정화 대기**: 1-2 RC 후 ureq/tokio_stream 호환 안정화 예상. 2026 Q3/Q4 추정
- **(c) Plugin 4-계층** (큰 사이클): ONNX 없이 auto memory + provider-auto-config + marketplace. ecosystem 안정성 의존성 낮음
- **(d) Kompress-back** (low priority): ONNX 없이 stemming 개선. v1 의 한계 우회

### 외부 blocker (계속)
- TASK-002, Gitea PAT, MiniMax OAuth, OpenAI/Google OAuth, ANTHROPIC_API_KEY, §5.12 디렉토리 자동 생성

### 다음 행동
1. **v2.0 후보 결정** — yklee 결정 시
2. tract (Pure Rust) 대안 검증 (ort 안정화 vs tract 의 trade-off)
3. 외부 blocker 우선 해결

## 13. D-67 (2026-06-10) — TASK-005-2 v2.0 tract Commit 1

### 결정 배경
- D-66 abort 후 tract (Pure Rust) 로 전환 결정 (yklee)
- library 분석: tract 0.23.0 (2026-06-01 stable, MSRV 1.91, Pure Rust, Sonos production)
- **D-66 lesson 적용**: 1차 cargo build 즉시 검증 (library 보고 의존 ❌)
- **build 결과**: **1차 build 20.82s 통과!** tract Pure Rust + workspace 호환 ✅

### tract 생태계 (2026-06 직접 확인)
- **latest stable**: 0.23.0 (2026-06-01), 0.22.0 (2025-08-26)
- **MSRV**: 1.91 (workspace 1.88 호환 ✅ — 1.91+ 의 build 가 1.88 caller 에서 OK)
- **Pure Rust**: no C++ toolchain, no ONNX Runtime binary download
- **License**: Apache 2.0 / MIT dual ↔ myharness MIT/Apache-2.0 호환
- **Production**: Sonos (wake-word, streaming ASR, LLM, TTS) — 검증된 안정성
- **all-MiniLM-L6-v2 호환**: standard ONNX opset, tract-onnx loader 지원
- **binary footprint**: 22-33MB (Pure Rust compiled, dev), but **release lto=thin 으로 13MB 유지** (verified)

### Commit 1 구현

#### Cargo.toml 추가
```toml
[dependencies]
# (W23 D-67) — tract 0.23 (Pure Rust, Sonos production)
tract = "0.23"
tract-onnx = "0.23"
dirs = { workspace = true }
tokio-stream = "0.1"

[dev-dependencies]
tempfile = "3"
```

#### onnx_model.rs (신규, 287 lines)
- `ModelInfo`: url, sha256, model_id, embedding_dim=384, max_tokens=256
- `ModelManager`: OnceLock lazy + Send+Sync, `new()` / `get()` global
- `cache_path`: `~/.cache/myharness/models/all-MiniLM-L6-v2.onnx`
- `ensure_downloaded`: reqwest streaming + sha2 SHA256 verify
- `load_runnable`: `tract_onnx::onnx()?.model_for_path()?.into_runnable()?` verify
- `embed()`: **Commit 2 stub** (Runnable 보관 API 가 Commit 1 한계)
- **Commit 1 한계**: tract 0.23 의 `Runnable(Arc<dyn trait>)` 가 type-safe 한 wrap 어려움 → Commit 2 에서 정착

### 5 L1 Test (PASS)
- `cache_path_under_myharness_models` — cache 경로 myharness/models 포함
- `sha256_of_known_data_is_correct` — b94d27b9 (hello world)
- `model_info_defaults_are_correct` — sha256 len=64, embedding_dim=384, max_tokens=256, url=HF
- `model_manager_new_is_not_loaded` — 새 instance not loaded
- `embed_stub_returns_err_before_session_loaded` — Commit 1 stub 검증

### 빌드 과정 (D-66 lesson 의 산 증거)

1. **1차 시도**: `tract = "0.23"` + `tract-onnx = "0.23"` → **build OK 20.82s** ✅
2. (구현 중 발생한 tract 0.23 API 발견):
   - `SimplePlan<F, O>` 는 tract-core, `InferenceSimplePlan` 은 tract-hir (= `SimplePlan<InferenceFact, Box<dyn InferenceOp>>`)
   - `tract_onnx::prelude::*` 가 InferenceFact re-export
   - `Runnable` 은 `pub struct Runnable(Arc<dyn trait>)` — type-safe 보관 어려움 → Commit 2 로 미룸
   - `OnceLock::get_or_try_init` 가 unstable `once_cell_try` feature → `set/get` 분리 패턴으로 우회
3. lib + test build 모두 통과

### 검증 결과
- `cargo build -p myharness-compression`: OK (20s 초기, 1m04s 재컴파일)
- `cargo test --workspace`: **429 passed / 0 failed / 2 ignored** (compression 45 = 40 baseline + 5 신규)
- `cargo clippy --workspace --all-targets`: **신규 warning 0건** (W9/W19-1/W18 기존 warning 만)
- `cargo build --release -p myharness`: 4m 07s, **binary 13MB** (release lto=thin 효과 — tract 의 Pure Rust 22-33MB 가 dev profile 영향만, release 최적화로 binary 영향 0)

### Commit + Dual-push
- **`f767a36`**: `feat(compression): W23 v2.0 tract Commit 1 — tract 0.23.0 + ModelManager skeleton (D-67)` (+1329 / -62)
- Gitea + GitHub push 완료 (20adbc5 → f767a36)

### 다음 행동
1. **TASK-005-2 v2.0 tract Commit 2** — actual `ModelManager::embed()` inference (tokenization + tract run + Runnable 보관 API). 추후 결정.
2. 외부 blocker 우선 해결 (TASK-002, Gitea PAT, OAuth, API key)
3. v2.0 다른 후보 (Plugin 4-계층, Kompress-back) 는 Commit 2 후 결정

### Side effect
- binary 13MB (release lto=thin 효과)
- tract Pure Rust 가 dev profile 22-33MB 추가 → binary size 디버깅 시 영향 (test 시)
- lib.rs 에 `pub mod onnx_model` 등록 — Commit 1 스켈레톤 public API
- workspace `sha2 = "0.10"` 추가 (tract ONNX model SHA256 verify 용, W21 hash8 과 통일)

## 14. D-68 (2026-06-10) — TASK-005-2 v2.0 tract Commit 2 abort

### 결정 배경
- D-67 Commit 1 (skeleton) 완료 후 actual `embed()` inference 구현 시도
- library 분석: tract 0.23 Runnable.run() + tokenizers 0.23 + ndarray → Tract Tensor
- D-67 의 한계 (Runnable 보관) 정착 시도

### 시도 중 발견한 tract 0.23 API 문제 (5+ error)

1. **E0425 cannot find Tensor in tract_data**: `tract_data::Tensor` 직접 노출 안 함. `tract_data::prelude::Tensor` 또는 `tract_data::tensor` (private module) 경로.

2. **E0603 module tensor is private**: `tract_data::tensor` module private. `plain_view` 만 public.

3. **E0308 mismatched types at line 224 (load_runnable)**:
   ```
   expected `Runnable`, found `Arc<SimplePlan<InferenceFact, Box<...>>>`
   ```
   `into_runnable()` 의 return 이 `SimplePlan<...>` (Runnable wrapper 아님). Wrap 필요:
   ```rust
   let arc: Arc<dyn tract_nnef::internal::Runnable> = Arc::new(plan);
   let runnable = tract::Runnable(arc);
   ```

4. **E0277 trait bound `tract::Runnable: tract_onnx::prelude::Runnable` is not satisfied**:
   - `tract_onnx::prelude::Runnable` (= `tract_api::Runnable`) 와 `tract::Runnable` 가 **다른 type** (wrapper vs re-export)
   - `runnable.run(...)` 직접 호출 불가
   - `Runnable::run(&*runnable, ...)` 또는 `(&**runnable).run(...)` 필요

5. **E0599 no method `to_array_view` on `&tract_onnx::prelude::Tensor`**:
   - `to_array_view` 가 `plain_view.rs` 의 method 인데, 그 안의 Tensor 가 **다른 type** (raw `tract_data::Tensor` vs `tract_api::Tensor` wrapper)
   - Deref/AsRef impl 없음 (`Tensor(Arc<InternalTensor>)` 의 field private)

### abort 결정
- **모든 변경 revert**: Cargo.toml (tokenizers 0.23, tract-data 0.23) + onnx_model.rs (embed() impl + mean_pool_l2_normalize + ensure_tokenizer_loaded)
- **working tree clean**: `git status` — modified 0 (ort/tract 모두 revert 후 의도적으로 깨끗)
- **D-67 skeleton 만 유지**: build OK, 5 L1 test, 429 workspace tests / 0 fail
- **binary 13MB 유지**

### 핵심 Lesson (D-68)
- **tract 0.23 의 low-level API 가 high-level inference 에 적합하지 않음** — `tract_onnx` 의 prelude 가 type-safe 한 wrap 을 노출 안 함
- **Runnable vs SimplePlan<...> 의 type 차이** 가 `into_runnable()` 후에도 runtime inference 에서 문제
- **Tensor wrapper 가 Deref/AsRef 없음** — ndarray 변환 위해 type cast 필요하나 unsafe
- **library 분석만으론 tract 0.23 의 inference API 정확히 파악 어려움** — D-66 lesson 과 같은 trap

### v2.0 ONNX 백로그 상태 (D-68 후)
- **ONNX 백로그 OOS** — tract 0.23 API 한계, ort ecosystem unstable, 두 ecosystem 모두 v2.0 ONNX 에 부적합
- v2.0 다음 후보:
  1. **Plugin 4-계층** (큰 사이클, ecosystem 안정성 의존 낮음)
  2. **Kompress-back** (low priority, ONNX 없이 stemming 개선)
  3. **외부 blocker 해결** (TASK-002, Gitea PAT, MiniMax OAuth, API key)

### 다음 행동
1. v2.0 후보 결정 — yklee 결정 시
2. 외부 blocker 우선 해결
3. tract 0.24+ 안정화 후 재시도 가능 (D-68 의 type 한계는 tract 0.23 한정)

(End of file)

## 15. D-65 (2026-06-10) — TASK-005-2 v1.5 종료 선언 (순서 보존, 정렬 정정)

### 결정 배경
- 6/10 하루 동안 v1.5 의 5 사이클 + D-62 정정 모두 완료 (W17, W18, W19-1, W20, W21, D-62)
- v1.5 후속 후보 (Plugin 4-계층, ONNX, Kompress-back) 의 실현 가능성 + 사이클 분류 검토
- **ONNX 통합** (3 background agent 분석) 은 가능하지만:
  - Initial_design.tt-3 의 '+10-30MB v1.5+' 설계 의도 따름 — v1.5+ 로 이미 분류
  - ort C++ build dep (CI/CD 복잡도)
  - binary 13MB → 18-23MB (+40-80%, 사용자 부담)
  - Layer 2 가 이미 opt-in rule-based 로 동작 중 (기능적 gap 없음)
- **v1.5 종료 선언** + v2.0 Planning 으로 ONNX 이관 결정

### v1.5 phase 누적 성과 (2026-06-09 ~ 2026-06-10)

| 사이클 | 결정 ID | 핵심 변경 | 신규 test |
|---|---|---|---|
| **W17** (D-60) | 비대화형 add-local (--url/--model/--token/--probe-skip) | register_local_provider_non_interactive fn + 4 clap flag + 3-mode 분기 | 4 L1 + 2 L2 (TC-W17-001~004 + I01/I02) |
| **W18** (D-61) | R-4 backup + Confirm + --yes | backup_providers_toml + backup 자동 + --yes flag + inquire::Confirm | 3 L1 + 2 L2 (TC-W18-001/002/003 + I01/I02) |
| **D-62** | W17 누락분 정정 (cross-check 오류) | state.json / handoff / backlog 정정 | 0 (메모리 정합성) |
| **W19-1** (D-62) | TC-W17-002 fail 복구 (AuthStore 주입) | register_local_provider_with_store + non_interactive_with_store | 4 L1 (TC-W17-002 수정 + TC-W19-001/002/003) |
| **W20** (D-63) | Ollama native /api/tags cascade | probe_local_models 2-stage + parse_ollama_tags + parse_openai_models | 3 L2 (TC-W20-I01/I02/I03) |
| **W21** (D-64) | sha256_8 + sort bug fix | hash8 util + backup filename `<ts>.<sha256_8>` + numeric sort | 3 L1 (hash8) + 4 L1 (TC-W21-001/002/003/004) |
| **누적** | | | **14 신규 test / 0 fail** |

### 검증 결과
- **cargo test --workspace**: 424 / 0 fail / 2 ignored
  - llm: 117 (110 baseline + 3 hash8 + 4 TC-W21)
  - w16_add_local: 10 (7 W16/W17/W18 + 3 W20)
  - tools/compression/tui/core/cli/context/auth: 297 lib
- **cargo build --workspace**: OK
- **cargo clippy --workspace --all-targets**: 신규 warning 0건 (W19-1/W18 기존 warning 만)
- **binary size**: 13MB (ort 통합 시 +5-10MB 예상 — v2.0 에서)

### v2.0 Planning backlog (3 후보)

#### v2.0-1: ONNX 3-commit (정공법)
- **Commit 1**: ort = "2" 의존성 + ModelManager lazy init + reqwest download to `~/.myharness/models/` + SHA256 verify
- **Commit 2**: CcrStore::compress_semantic (all-MiniLM-L6-v2 ONNX 43MB, 384 dim, cosine threshold grouping) — "Anthropic Claude" ↔ "anthropic claude 3.5" 같은 marker
- **Commit 3**: KompressOnnx (도메인 명사 보존, semantic compression) — stem 알고리즘 대체
- **Fallback**: ONNX init 실패 시 rule-based v1 로 graceful degradation
- **사이드**: 13MB → ~23MB binary (ort C++ static), 43MB model runtime download
- **사이클**: 3 작은 사이클, compression crate only

#### v2.0-2: Plugin 4-계층 (큰 사이클)
- auto memory + provider-auto-config skill 정식 + marketplace + plugin installer
- CONCEPT.md §5.14 Skill/MCP first-class 구현
- **cross-crate 영향**: core/llm/tools/compression/cli 모두 영향
- 큰 사이클 (multi-cycle)

#### v2.0-3: Kompress-back (low priority)
- kompress_v1 의 stemming 1자 제거를 porter-stemmer-lite 같은 suffix stripping 으로 교체
- ONNX 없이 도메인 명사 stem 문제 일부 완화
- 우선순위 낮음 (Layer 2 opt-in, 사용 빈도 낮음)

### 외부 blocker (계속)
- TASK-002: 도메인별 명령 가이드 (yklee 인프라 정보 의존)
- Gitea PAT 회전 (이전 세션 노출)
- MiniMax Device OAuth real flow 검증 (W15.b 자동 refresh real test 가능)
- OpenAI/Google OAuth client_id 등록
- ANTHROPIC_API_KEY 주입 시 LLM E2E 테스트
- §5.12 디렉토리 자동 생성 (v1 first run 시)

### 다음 행동
1. **TASK-005-2 v2.0 진입 결정** — yklee 결정 시
2. 후보: (1) ONNX 3-commit / (2) Plugin 4-계층 / (3) Kompress-back
3. 외부 blocker 우선 해결 (TASK-002, Gitea PAT, MiniMax OAuth)
4. v1.0 + v1.5 산출물 안정화 후 v2.0 진입

## 16. 종합 정리 (2026-06-10) — 하루 작업 전체 요약

### Commit Timeline (D-62 → D-68)

| 시간 | 결정 | Commit | 핵심 |
|------|------|--------|------|
| 아침 | D-62 | `339938b` | W17 누락분 = 0건 정정 (cross-check 오류) |
| 오전 | D-62 W19-1 | `8fe2948` | `register_local_provider_with_store` (AuthStore 주입) — TC-W17-002 fail 복구 |
| 오전 | D-63 | `7667bd9` | `probe_local_models` 2-stage cascade (Ollama native → OpenAI compat) |
| 오후 | D-64 | `70ae771` | `hash8` (sha256 8-char) + `cleanup_old_backups` sort bug fix (string → numeric) |
| 오후 | D-65 | `392716b` | v1.5 종료 선언 + v2.0 Planning backlog 등록 |
| 저녁 | D-66 | `20adbc5` | v2.0 ONNX Commit 1 abort (ort ecosystem unstable, 5 시도 빌드 깨짐) |
| 저녁 | D-67 | `f767a36` | v2.0 tract Commit 1 (Pure Rust, Sonos production) — skeleton + 5 L1 PASS |
| 저녁 | D-68 | `5a69b47` | v2.0 tract Commit 2 abort (Tensor wrapper, Runnable mismatch, 5+ error) |

### 누적 성과 (2026-06-10 하루)

- **코드 commit**: 4 (W19-1 + W20 + W21 + tract Commit 1)
- **메모리 commit**: 8 (D-62 / D-62 W19-1 / D-63 / D-64 / D-65 / D-66 / D-67 / D-68)
- **신규 test**: 14 (W19-1 4 L1 + W20 3 L2 + W21 4 L1 + hash8 3 L1)
- **workspace tests**: 411 → 424 → **429** / 0 fail / 2 ignored
- **compression lib tests**: 40 → 45
- **binary size**: 13MB (release lto=thin)

### 결정 ID 매트릭스 (4 memory 파일 모두 일치)

```
D-62          : W17 누락분 = 0건 정정
D-62 W19-1    : AuthStore 주입 패턴 (TC-W17-002 fail 복구)
D-63          : Ollama native /api/tags cascade
D-64          : sha256_8 + sort fix (W21 F-1+F-2)
D-65          : v1.5 종료 선언
D-66          : v2.0 ONNX Commit 1 abort (ort ecosystem)
D-67          : v2.0 tract Commit 1 (skeleton)
D-68          : v2.0 tract Commit 2 abort (tract API 한계)
```

### 메모리 파일 일관성 (D-66 검증 결과)

- **decided[]**: 24 (D-62~D-68 모두 7 row + D-62-W19-1 별도)
- **deferred[]**: 10 (v2.0 후보 3개, TASK-002, OAuth 3개, MYHARNESS_MINIMAX_CN, TASK-005-2-W17-W18-PR DONE, TASK-005-2-W19+ 종료)
- **next_actions**: 17 → 7 (open items 만, closed 결정 정리)
- **recent_done**: 21 (D-66/D-65/D-64/D-63/D-62 모두 등장)
- **in_progress**: 3 → 5 (D-59/D-65/D-66 + D-67/D-68 추가 — milestone anchor)
- **D-62~D-68 모든 결정이 4 파일에 등장** (state.json / handoff / backlog / 2026-06-10.md)
- **D-62-W19-1 결정 표 row 중복 발견 → 정정**
- **2026-06-10.md §11 (D-65) 순서 깨짐 → §15 로 renumber**
- **2026-06-10.md §15 중복 발견 → line 538 삭제**

### 두 abort 의 lesson (D-66 + D-68)

1. **D-66 (ort)**: ecosystem stability 는 SSOT. library 분석 시 crates.io + lib.rs + **실제 cargo build** 검증 필수. library 보고만 의존 ❌. 1.x 전부 yanked, 2.0.0-rc.x 전부 ureq 3.1 API 변경.
2. **D-68 (tract)**: tract 0.23 의 low-level API 가 high-level inference 에 부적합. `Tensor(Arc<InternalTensor>)` wrapper field private + Deref 없음 + `to_array_view` 가 `plain_view::Tensor` 의 method. type-safe 한 wrap 어려움.

### 다음 세션 시작점 (D-68 후)

- **in_progress (5)**: D-59 v1 MVP 종료, D-65 v1.5 종료, D-66 v2.0 ONNX abort, D-67 v2.0 tract Commit 1, D-68 v2.0 tract Commit 2 abort
- **next_actions (7)**: TASK-005-2 v2.0 진입 결정, OAuth 3건, ANTHROPIC_API_KEY, §5.12 디렉토리, TASK-002, Gitea PAT
- **deferred (10)**: v2.0 후보 3개 (ONNX / Plugin / Kompress-back) + 7 외부 blocker

