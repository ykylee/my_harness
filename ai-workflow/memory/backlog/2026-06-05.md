# 2026-06-05 작업 백로그

- 문서 목적: my_harness 부트스트랩 당일의 구체적인 작업 계획과 실행 결과를 기록한다.
- 범위: 2026-06-05에 수행한 my_harness 표준 워크플로우 도입 작업
- 대상 독자: yklee, Mavis orchestrator, 다음 세션 에이전트
- 상태: in_progress
- 최종 수정일: 2026-06-05
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [Project Profile](../../../docs/PROJECT_PROFILE.md)

## TASK-001 my-harness 부트스트랩 (standard_ai_workflow 적용)

- 상태: done
- 모드: Implementation
- 우선순위: high
- 담당: yklee (Mavis 지원)
- 영향 문서: `MiniMax.md`, `MiniMax_config.example.json`, `.MiniMax/agents/`, `docs/PROJECT_PROFILE.md`, `ai-workflow/memory/`, `ai-workflow/core/`

### 계획 (Plan)
- 작업 목표: `standard_ai_workflow` (ykylee/standard_ai_workflow) 의 워크플로우를 `my_harness` 저장소에 적용. minimax-code 하네스 오버레이 포함.
- 완료 기준:
  - `bootstrap_workflow_kit.py` 로 minimax-code 오버레이 적용 완료
  - `MiniMax.md` 진입점 생성
  - `.MiniMax/agents/workflow-*.md` 5종 (orchestrator/worker/doc/code/validation) 생성
  - `ai-workflow/memory/state.json` 정상 생성 확인
  - `docs/PROJECT_PROFILE.md` yklee 하네스 컨텍스트로 커스텀

### 실행 (Act)
- 진행 현황:
  - `2026-06-05 21:01` `git init -b main` 으로 저장소 초기화
  - `2026-06-05 21:02` `bootstrap_workflow_kit.py` 실행 → minimax-code 오버레이 적용
  - `2026-06-05 21:02` `ai-workflow/scripts/generate_workflow_state.py` 로 `state.json` 재생성 → `status: ok`
  - `2026-06-05 21:04` `docs/PROJECT_PROFILE.md` TODO 제거 및 하네스 컨텍스트 채움
  - `2026-06-05 21:05` `ai-workflow/memory/session_handoff.md` 초기화 (현재 세션 베이스라인)
- 수정 사항: 없음 (1회 실행으로 부트스트랩 완료)

### 검증 (Validate)
- 테스트 결과: `generate_workflow_state.py` 실행 → `state_cache_status: refreshed` 확인
- 미실행 사유: `ai-workflow/tests/check_*.py` 는 소스 프레임워크 레이아웃 가정 — 컨슈머 환경에서는 추후 재작성 검토. 본 TASK 의 본질적 완료 기준은 충족.

### 결과 (Result)
- 작업 결과: my_harness 저장소에 minimax-code 표준 워크플로우 적용 완료. 다음 세션은 `MiniMax.md` 를 진입점으로 세션 시작 가능.
- 다음 세션 시작 포인트: `MiniMax.md` → `ai-workflow/memory/state.json` → `session_handoff.md` → `work_backlog.md` → `docs/PROJECT_PROFILE.md` 순서로 읽고 세션 복원.
- 남은 리스크 및 후속 작업:
  - `MiniMax.md` 의 TODO 명령 5종 채우기
  - `.MiniMax/config.json` 초기화 (시크릿 환경변수 주입)
  - 첫 실제 작업 발생 시 본 일별 백로그에 TASK 등록

---

## TASK-002 [차기] 하네스 워커 smoke check 컨슈머 보정

- 상태: planned
- 모드: Refactoring
- 우선순위: medium
- 담당: yklee (Mavis 지원)
- 영향 문서: `ai-workflow/tests/`

### 계획 (Plan)
- 작업 목표: 현재 `ai-workflow/tests/check_*.py` 가 `workflow-source/` (at root) 레이아웃을 가정. 컨슈머(`ai-workflow/` only) 레이아웃에서 동작하는 별도 smoke 또는 path override 추가.
- 완료 기준: `for t in ai-workflow/tests/check_*.py; do python3 "$t"; done` 가 0 종료코드 또는 알려진 스킵 사유만으로 종료.

---

## TASK-003 [차기] 도메인별 명령 가이드 작성 (코드 / 서버 / 환경)

- 상태: planned
- 모드: Requirements → Design
- 우선순위: medium
- 담당: yklee (Mavis 지원)
- 영향 문서: `MiniMax.md` §"프로젝트 실행 기본값", `docs/PROJECT_PROFILE.md` §3.1

### 계획 (Plan)
- 작업 목표: 2026-06-05 확정된 3개 도메인 (코드 개발 / 서버 관리 / 환경 셋업) 각각에 대해 표준 명령/진입점 가이드를 `MiniMax.md` 와 `docs/PROJECT_PROFILE.md` 에 채운다.
- 완료 기준:
  - **코드 개발**: 대상 프로젝트 타입별 (Next.js / Go 백엔드 / Python 등) install / test / lint / run 표준 명령. gh CLI / vitest / Playwright 활용 패턴 명시.
  - **서버 관리**: 원격 호스트 목록 (또는 `~/.ssh/config` 별칭 참조 경로), 헬스체크 / 로그 / 설정 변경 / 배포 헬퍼 표준 명령. 위험 작업 전 사용자 승인 규칙 재확인.
  - **환경 셋업**: Homebrew 패키지 목록 / 런타임 버전 매니저 / dotfiles 저장소 / 신규 머신 부트스트랩 단일 진입점. idempotent 확인 절차 포함.
  - 각 도메인 명령은 `safe to run without approval` vs `requires explicit approval` 으로 분류.

### 실행 (Act)
- 진행 현황: yklee 의 개인 인프라/도구 셋업 정보가 필요해 본 세션에서는 TODO 로 남김. 다음 세션에서 1:1 으로 도메인별 표준 명령 정의.

### 결과 (Result)
- 다음 세션 시작 포인트: 3개 도메인 중 우선순위가 높은 것 (제안: 환경 셋업 → 가장 빈도 낮지만 안전 마진 큼) 부터 도메인 표준 명령을 1개씩 채우기.

---

## TASK-004 CLI/TUI 툴 레퍼런스 5종 비교 분석

- 상태: planned
- 모드: Analysis
- 우선순위: high
- 담당: yklee (Mavis 지원)
- 영향 문서: 본 세션에서 `docs/REFERENCES.md` 신규 (분석 결과 요약), 이후 디자인 결정의 입력

### 계획 (Plan)
- 작업 목표: 2026-06-05 클론한 5개 오픈소스 하네스를 비교 분석해, 우리가 만들 CLI/TUI 툴의 아키텍처 결정을 위한 입력 자료로 만든다.
- 클론 위치: `/Users/yklee/repos/harness-refs/`
- 분석 축 (각 레퍼런스 × 각 축을 1줄 이상 채움):
  1. **언어 / 빌드 시스템** — 우리 툴의 스택 선택 후보
  2. **TUI 라이브러리** — bubbletea / ink / ratatui / Textual / 직접 구현 등
  3. **Cross-platform 전략** — Windows / Linux / macOS 빌드/배포 파이프라인
  4. **에이전트 토폴로지** — 단일 루프 / orchestrator-worker / 멀티 에이전트
  5. **컨텍스트 관리** — 코드베이스 색인, 파일 읽기 전략, 토큰 절약 패턴
  6. **세션 / 상태 영속화** — 채팅 로그, 체크포인트, resume 지원
  7. **확장 포인트** — 플러그인 / MCP / 커스텀 도구 등록 방식
  8. **표준 워크플로우 호환성** — AGENTS.md / state.json / handoff 패턴 사용 여부
- 완료 기준:
  - `docs/REFERENCES.md` 1차 작성 — 5개 × 8축 비교표 + 우리 방향성 초안
  - yklee 가 1차 보고 듣고 픽한 2~3개에 대해 deep-dive

### 실행 (Act)
- 진행 현황:
  - `2026-06-05 22:05` 5개 클론 (OpenCode / Aider / Codex CLI / Goose / Gemini CLI), `--depth 1`
  - `2026-06-05 22:06` 클론 검증 — 1.1GB, 각 repo README/구조 확인
  - `2026-06-05 22:12` Gitea repo 5개 생성 (POST /api/v1/user/repos, private, auto_init=false). 첫 push 시도 → 모두 shallow 거부
  - `2026-06-06 20:34` 각 clone 에 `upstream`(GitHub) 추가 + `git fetch --unshallow upstream` 5개 병렬 실행. 이후 Gitea 재 push 전부 성공
  - `2026-06-06 20:34` origin = Gitea (clean URL), upstream = GitHub 으로 듀얼 remote 구조 확립. upstream URL 매핑은 `/Users/yklee/repos/harness-refs/.upstream-urls` 에 저장

### 결과 (Result)
- 다음 세션 시작 포인트: `docs/REFERENCES.md` 1차 분석 (축 1~4 우선, 그 다음 5~8).

---

## TASK-005 my_harness 의 CLI/TUI 툴 전환 (방향 확정 후)

- 상태: planned
- 모드: Planning (Analysis → Requirements → Design → Implementation)
- 우선순위: high (방향 확정 즉시 착수)
- 담당: yklee (Mavis 지원)
- 영향 문서: `my_harness/` 전반 — 현재 standard_ai_workflow 기반 워크플로우 구성을 새 CLI/TUI 툴의 워크플로우 레이어로 재편

### 계획 (Plan)
- 작업 목표: `my_harness` 저장소를 단순 워크플로우 컨슈머가 아니라 *yklee 가 직접 개발/배포하는 CLI/TUI 코딩 에이전트* 의 소스 트리로 전환.
- 전제:
  - 사용 플랫폼: Windows / Linux / macOS 동시 지원
  - 표준 AI 워크플로우 (state.json / handoff / backlog / minimax-code 오버레이) 준수
  - 3-도메인 (코드 개발 / 서버 관리 / 환경 셋업) 진입점 제공
- 결정 보류 (TASK-004 결과 대기):
  - 언어 (Rust / Go / TypeScript / Python)
  - TUI 라이브러리
  - 패키징 / 배포 채널 (Homebrew / scoop / cargo / npm / winget / 단독 바이너리)
  - 라이선스
  - 이름 (현재는 `my_harness` 임시; 공식 명칭 미정)
- 완료 기준: TASK-004 완료 + yklee 와 1:1 디자인 리뷰 후 본 TASK-005 의 세부 단계 (TASK-005.1 스택 결정, TASK-005.2 MVP 범위, …) 로 분해.
